"""contamlab — LLM ベンチマークの汚染を、検定つきで測る。

ベンチマークスコアは「能力 + 記憶」の合計値であって、能力そのものではない。
この2つを分離するには仮説検定が要る。

既存の汚染研究のほとんどは「摂動版で N% 落ちた」で止まっており、検定・信頼区間・
検出力分析・多重比較補正が無い。contamlab はそこを埋めることだけを目的にしている。

外部依存を持たない(標準ライブラリのみ)。統計層が成果物の中心なので、
式が全部読める状態にしてある。
"""

__version__ = "0.1.0"
