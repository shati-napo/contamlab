# ラン `jmmlu-shuffle-02` の報告 —— **中止**(2026-08-06)

**結論を先に書く。このランは「JMMLU が汚染されているか」に答えていない。**
答えられなかった、というのが結果である。事前に決めた参加条件を満たすモデルが
2本揃わず、**検査そのものを実行しなかった。**

**HOLDOUT 1,922 問は未使用のまま保全されている(K = 0 / 10)。**

一次記録は [preregister.md](../preregister.md) の「ラン: jmmlu-shuffle-02」節。
本稿はその要約であり、数字が食い違ったら preregister が正である。

---

## 1. 何を測ろうとしたか

日本語ベンチマーク **JMMLU**(53科目・6,664問)について、
**「スコアが能力なのか、問題を覚えているだけなのか」**を分離する。

方法は**摂動法**である。選択肢の並び順だけを入れ替え(`shuffle_choices`)、
中身が同じ問題で正答率がどれだけ落ちるかを見る。理解して解いているなら
並び順は効かない。位置ごと覚えているなら落ちる。

| 設計 | 値 |
|---|---|
| 分割 | DEV 4,742 問 / **HOLDOUT 1,922 問**(非公開シードで固定) |
| 摂動器 | `shuffle_choices`(K の上限 10 のうち 1 本目) |
| 狙う効果量 | 5 pt の低下 |
| 検定 | McNemar(対応あり)+ Clopper-Pearson の下限 + Holm 補正 + Cochran の Q |
| ロースター | `llm-jp-3-13b-instruct3` / `Llama-3.1-Swallow-8B-Instruct-v0.5`(M=2) |
| 測定条件 | `mmnga` の GGUF / Q4_K_M / temperature 0 / max_tokens 256 / `OLLAMA_NUM_PARALLEL=1` |
| 実行系 | Lambda の A100 40GB 上の Ollama 0.32.6。**問題文はホストの外に出していない** |

**この報告書で最も大事な設計は、判定の基準を測る前に全部書いてあることである。**
必要問題数も、モデルの参加条件も、途中で止める条件も、
[preregister.md](../preregister.md) に日付つきで固定してある。以下はその適用の記録にすぎない。

## 2. なぜ止めたか

本番(HOLDOUT)の前に、DEV 70 問で**モデルの参加条件**を確かめるパイロットを置いていた。
条件は測定の前に宣言済みで、そのうち1つが「**解釈不能率が 5% を超えたモデルは外す**」である。

解釈不能とは、モデルの応答から「どれを選んだか」が読み取れないことを指す。
読み取れない応答は不正解として数える設計なので、これが多いモデルは
**正答率が能力を反映しなくなり、その低下を汚染と読むこともできなくなる。**

| モデル | 素の正解率 | 解釈不能率 | 判定 |
|---|---|---|---|
| `swallow31-8b` | 0.5143 | **1.4%** | ✅ 条件を満たす |
| `llmjp3-13b` | 0.3143 | **37.1%** | ❌ **5% 超で脱落** |

生存 **1 本**。事前確約の「**残り2本未満なら本番を実行しない**」に該当した。

**理由は検出力ではない。** 1本でも統計的な検出はできる。できないのは
**採用基準4(不均一さ)**である —— 「汚染が特定のモデルにだけ出る」ことを示すには
Cochran の Q が要り、これは 2 本以上でしか計算されない
([harness.py:188-192](../contamlab/harness.py#L188-L192) が `len(results) >= 2` でガードする)。
1本では、どれだけ大きな低下が出ても「**摂動で問題が難しくなっただけ**」と区別できない。
区別できないものを「汚染」とは書けない。

**差し替え探索にも戻らなかった。** 母集団(7B 以上 / instruct 版 / `mmnga` の Q4_K_M /
llm-jp-eval で JMMLU を採点される)を満たす候補は 6 本まで数え上げてあり、既に枯れている。
残る候補を採るには母集団の定義を緩める必要があり、それは**結果を見てから主張範囲を変える**
ことなので、しない。

## 3. `llmjp3-13b` は何に落ちたか —— 2回間違えた

**当初の説明は「モデルが指示に従わないから」だった。半分は正しく、半分は誤っていた。**
報告としてはこの経緯ごと残す。

contamlab は問題文の末尾に「正しい選択肢の記号だけを答えてください。」と書く。
`swallow31-8b` は 70 問中 69 問を **1 文字**で返した。`llmjp3-13b` は長文で答えた。
ここまでは事実である。

しかし 2026-08-07 に解釈不能 26 件を採点器へ通し直して形を数えたところ:

| 26 件の内訳 | 件数 |
|---|---|
| **先頭に答えラベルがあるのに拾えていなかった**(`正解はDです。…`) | **15** |
| 接頭辞だけでラベルが無い / 推論のみ / 打ち切り | 11 |

原因は採点器の正規表現がラベル直後に**日本語を許していなかった**ことである
(「正解は**D**です」の「で」で外れる)。docstring は「`答えはA`」を拾うと宣言していたので、
**宣言と実装が食い違っていた。** これは装置の穴であって、モデルの落ち度ではない。

**それでも判定は変わらない。** 15 件を全部救っても解釈不能率は **11/70 = 15.7%** で、
事前条件 5% を依然超える。中止・K=0・HOLDOUT 未使用はすべて不変である。

| モデル | 解釈不能率(修正前) | 解釈不能率(修正後) | 事前条件 5% |
|---|---|---|---|
| `swallow31-8b` | 1.4% | 1.4%(変化なし) | ✅ |
| `llmjp3-13b` | 37.1% | **15.7%** | ❌ 依然超過 |

装置は修正済み(`5851dc2`)だが、**このランの数値は置き換えていない**(併記のみ)。
修正が効くのは次のランからである。経緯・根拠・残るリスクは
[preregister.md](../preregister.md) の変更履歴 2026-08-07 に全部書いた。

> **原因の説明を2回続けて外している。**
> 1回目は「停止トークン `<|im_end|>` の漏れだろう」(実測: 35 件すべて末尾に出現しており無関係)。
> 2回目は「ラベルを末尾に置く長文だから」(実測: 15 件は先頭にラベルがあった)。
> **判定は2回とも動かなかった。** 判定を「解釈不能率という事前に固定した1つの数字」に
> 委ねてあったからで、原因の説明は事後の解釈にすぎない。
> **この設計が効いた場面として記録しておく。**

## 4. 何が言えて、何が言えないか

**言えること。**

- `Llama-3.1-Swallow-8B-Instruct-v0.5` は、この書式の JMMLU で正答率 0.514、
  解釈不能率 1.4%。参加条件を満たした
- `llm-jp-3-13b-instruct3` は、**この書式では**測定できなかった

**言えないこと(重要)。**

- **JMMLU が汚染されているか、していないか。** 検査を実行していない
- **`llm-jp-3-13b-instruct3` が JMMLU を解けないこと。**
  落ちたのは「記号だけを答える」という**contamlab の出力書式**との相性であって、
  能力ではない。llm-jp-eval の標準ハーネスは別の書式を使う。
  **このモデルの JMMLU スコアが低いという主張は、本稿からは導けない**
- **上の 2 モデルの比較。** 片方が脱落した時点で対照が成立していない

## 5. 次に決めること

再開するなら、**測る前に**決める必要があるのはこの1点である。

> contamlab のプロンプトは「記号だけ」を要求する。**長文で答えるモデルを弾く。**
> 母集団を「この書式で答えられるモデル」に絞るのか、書式を変えて対象を広げるのか。

書式を変えれば `llmjp3-13b` は救えるかもしれないが、それは
**「落ちたモデルが通る条件を、落ちたのを見てから選ぶ」**ことでもある。
どちらを選ぶにせよ、**結果を見る前に** [preregister.md](../preregister.md) に書いてから動かす。
既存の測定値(ψ = 0.4050 など)は書式が変われば引き継げない可能性がある点にも注意する。

---

## 付録: 数字の出どころ

| 値 | 出どころ |
|---|---|
| パイロット①の結果 | `reports/pilot1.lambda-a100-40gb-20260806.json`(gitignore 対象・ローカルのみ) |
| 生の応答 140 件 | `data/cache/responses.lambda-a100-40gb-20260806.jsonl`(同上) |
| ベンチマークの由来(53科目・7,097行から 6,664 問を採用) | [data/jmmlu.manifest.json](../data/jmmlu.manifest.json)(**git 追跡下**。取得元 commit `762cbf19` と各 CSV の sha256 が入っている) |
| 分割 DEV 4,742 / HOLDOUT 1,922 | 上の manifest + 非公開シード(シードは配らない) |
| 実行環境(GPU / Ollama 版 / GGUF の SHA256) | [preregister.md](../preregister.md) の「実行環境」節 |
| 再採点後の解釈不能率 | 上のキャッシュを修正後の `select` に通し直した実測(2026-08-07) |

`reports/` と `data/cache/` は **gitignore 対象**なので、リポジトリには入っていない。
**数字そのものは preregister.md に転記済み**であり、そちらが公開側の一次情報である。
