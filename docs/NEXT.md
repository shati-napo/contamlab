# NEXT — 次の一手(2026-08-16 更新)

> このファイルが**再開点**。ここから読み、終わったらここを更新する。
> 一次情報はリポジトリ、Obsidian の [[contamlab]] はその索引。数字が食い違ったらこちらが正。

## いまの状態

- **公開済み** — https://github.com/shati-napo/contamlab(2026-08-08 に PUBLIC へ切替)
  - 切替前に `git ls-files` と `git log --all --diff-filter=A` を再走査し、追跡・全履歴とも
    `.pem` / `.env` / `.key` の混入ゼロを確認。追跡下の `.jsonl` は公開用の架空8問
    [data/example.jsonl](../data/example.jsonl) のみ
- テスト 408 件通過 / `contamlab verify` 通過 / CI 8構成(OS 2 × Python 4)success(いずれも最新コミット時点)
- **HOLDOUT は消費済み(K = 1/10)。** 同じ HOLDOUT で再検定はできない
- **この装置はまだ一度も「汚染あり」を出していない。** 陽性対照が無いため。公開したことで
  この穴は誰にでも見える状態になった
- **陽性対照の試行は 5 ラン目まで全敗**(pc-01 停止 / pc-02 第0段で停止 / pc-03 ベース確定 /
  pc-04 梯子 全滅 / **pc-05 F1・F2 不合格・F3 は条件節で未実行**)。
  **測定(較正曲線・検出下限)には一度も進んでいない。HOLDOUT は開けていない**
- 累計の GPU 費用は pc-05 で **約 $16.5** 追加(ラン単位の上限 $45 は超えていない)
- **▶ `positive-control-06` を 2026-08-16 に事前登録し、同日 実行して L0 の関門で停止した。**
  **動かす軸は推論時の LoRA スケール λ だけ。学習はラン全体で1本。** 下の pc-06 の節が正
- ★ **pc-06 の結果: L0(λ=1.0)が pc-04 R1 の再現帯を外れ、λ の梯子は1段も下ろしていない。**
  非注入群 正解率 **0.5700**(帯 [0.384, 0.481])/ 解釈不能率 **8.25%**(帯 [22.9%, 31.6%])/
  差 **+18.25pt**(+8.4pt 以上は満たす)。**学習は再現した**(train_loss 0.2763194784 が一致)
  **のに測定値が動いた** → ズレはマージか量子化の段。**原因は未特定**
- ★ **路線の終了条件を先に固定した** —— **pc-06 で合格する段が無ければ陽性対照の自作を打ち切り、
  `positive-control-07` は作らずステップ3(書く)へ移る**。
  ★ **ただし本ランは「梯子を回した末の全滅」ではなく「土台の再現に失敗して停止」**であり、
  この終了条件には**該当しない**。**次の一手は改めて事前登録する**(下の節)
- **▶ 次は `merge-variance-01`(2026-08-16 事前登録・未実行)。★ 陽性対照のランではない。**
  測るのは「**同一アダプタ・同一環境で複製を3本作ったときの解釈不能率の広がり R**」だけ。
  **R ≥ 9.5pt なら pc-04・pc-05・pc-06 の段ごとの合否は読み直しが要る**という判定表を
  **測る前に固定済み。**上限 $15。**★ 着手点は実装(課金ゼロ)から**

## 事業性の調査結果(2026-08-08・結論のみ)

**単体の商売にはならない。評判資産としては強い。**

- 手法は既知(Oren et al. 2024 / DCQ (TACL) / [PaCoST](https://arxiv.org/abs/2406.18326))
- 「統計的に厳密」の旗も先行あり([FTD, ACL 2026](https://aclanthology.org/2026.acl-long.1390/)、conformal 版 2507.13835)
- **日本語では [Nejumi Leaderboard 4](https://nejumi.ai/)(W&B Japan)が同じ JMMLU を
  3パターン(通常・記号置換・不正解選択)で既に無料検査している**
- 稼げているのは「検出器」ではなく「**非公開の問題を持っていること**」(Vals AI / Scale SEAL /
  ARC-AGI / FrontierMath / HLE)。**contamlab は問題を持っていないのでこの形には乗れない**
- 残った本物の差分は2つだけ: **(a) logprob 不要のブラックボックス**(正答率の低下だけを見るので
  確率を返さない API 専用モデルに使える)**(b) 検出器ではなく「規律」**(他は3つのスコアを
  並べるだけで「その差は区別できるのか」「見えるだけの n だったか」を言っていない)

---

# ステップ1(2026-08-08 完了): Nejumi の問題ごとの正誤 → **取れた**

**結論と取得手順は [nejumi4-per-sample-2026-08-08.md](nejumi4-per-sample-2026-08-08.md) にある。**

成功条件3つ(問題ごとの正誤 / 3パターンが同一問題集合 / モデル横断)はすべて揃った。
公開 W&B プロジェクト `llm-leaderboard/nejumi-leaderboard4` の run ファイルとして
1 問 1 行・3パターン結合済みのテーブルが置かれており、**API キーも `wandb` パッケージも不要**で
HTTP だけで落ちる(標準ライブラリ限定の鉄則を破らずに済む)。確認した 6 モデルで問題集合は完全一致。

覚えておく制約: **n = 100**、**2-shot のみ**、科目別の内訳は無し。
テーブルの `score` 列は正誤ではなく 3 出力の整合性(1.0/0.5/0.0)なので、正誤は自分で導出する。

**分岐の指示どおり、ここで寝かせる。** 着手はステップ2の後。
較正済みの装置を持ってから読むほうが、立場が「他人の数字へのダメ出し」にならない。
測定はしていない。使うときは**先に事前登録**。

---

# ▶ ステップ2(進行中・本命): 陽性対照を自分で作る

> **事前登録は 2026-08-08 に完了した(1問も測る前)。**
> [preregister.md](../preregister.md) の「## ラン: positive-control-01」が正。
> 設計・判定規則・事前予測・停止条件・主張範囲は**すべてそこに固定済みで、後から変えない。**
>
> | 確定した設計 | 値 |
> |---|---|
> | ベース | `Qwen/Qwen2.5-1.5B-Instruct` |
> | アーム M | **6**(注入率 0 / 2 / 5 / 10 / 20 / 40%・**入れ子**) |
> | 測定問題数 n | **4,742**(DEV 全量。HOLDOUT も K も消費しない) |
> | 摂動器 / 書式 | `shuffle_choices` / 書式 C |
> | 検出下限 | ψ=0.4050 で **2.99pt**(ψ が 0.80 でも 5pt は見える) |
> | 事前予測 | drop ≒ 0.575 × X、**検出下限は注入率 5.20%** |
> | 判定 | A 0%で偽陽性なし / B 40%で陽性 / C Cochran の Q が有意 |
>
> **★ このランは 2026-08-09 に「操作チェックで停止」で終了した。** 下の
> 「## ラン positive-control-01 の結末」を読むこと。**測定(56,904 コール)は1問も撃っていない。**

## ラン positive-control-01 の結末(2026-08-09)—— **★ 関門で停止。測定に進まなかった**

```
pc-x40  (注入率 40%)
    非注入群 n=400  正解率 0.3250  解釈不能  9.50%
    注入群   n=400  正解率 0.4050  解釈不能 11.75%
    差       +0.0800  (+8.00 pt)      ← 停止条件「X ≥ 20% で 10pt 未満」に該当
```

規則どおり**残り5アームは作らず、`70-positive-control.sh` は実行していない。**
`reports/manipulation-check.<tag>.ok` が無いので仕組みとしても起動できない。
**`drop` / `p_value` / `adjusted_lcb` は一度も見ていない。**

> [!important] **読めるのは「注入が入らなかった」だけ。**「装置が鈍い」とは読まない。
> 検出器の性能については、このランは**何も言っていない。**

**学習そのものは設計どおり通っている** —— ここが次の設計の出発点になる。

| 実測 | 値 | 凍結値と |
|---|---|---|
| 総内容トークン T | 2,831,004 | **完全一致** |
| 注入問題数 × E | 1,896 × 12 | 一致 |
| ブロック数 / ステップ | 1,460 / 91 | — |
| train_loss | 1.2416 | — |
| GGUF sha256 | `54d866cfee1c3daa1430ac55412b4ef11b84755d2bb65501176f8f3f70ddb21c` | — |
| Ollama | 0.32.6 / llama.cpp `b10327` | pin どおり |

**T もEも設計値ちょうどで、それでも 8pt しか動かなかった。**
記録一式は [reports/positive-control-01/](../reports/positive-control-01/) に回収済み
(environment / train.json / gguf-sha256 / 操作チェックログ / 学習ログ)。
GPU(Lambda A100-SXM4 40GB)は terminate 済み。実費 **約 2.5 時間 × $1.99 ≒ ¥750。**

### 走らせて分かった実装側の3点(いずれも凍結値ではない。preregister に追記済み)

1. **A10 24GB では登録済みの `batch 8 × 2048` が原理的に載らない。** 語彙 151,936 の
   ロジットだけで bf16 4.98GB →`.float()` 9.96GB → shift のコピー 9.27GB。A100 40GB に変更した
2. `train_lora.py` が T をレコード区切りの **EOS 込み**で数えていた(40% アームだけ 1,896 tok 超過)。
   内容トークンだけに直した。凍結値 235,917 は EOS 抜きの値で、修正後ちょうど一致した
3. `to_gguf.sh` の `python` が venv 外を見る / 変換の Ollama 登録段階では
   Ollama が起動している必要がある(学習中は VRAM のため止める)

## ラン positive-control-02(2026-08-09)—— **★ 第0段で停止。梯子は1本も走らせていない**

> **結末を先に書く。** 未 fine-tune のベースを書式 C で測ったところ、
> **正解率 0.3600(基準 ≥ 0.30 は通過)/ 解釈不能率 14.50%(基準 ≤ 5% を超過)。**
> 事前登録の判定表どおり **停止**。R0〜R4 は1本も学習していない。
> **E・学習率・rank は1つも触っていない。** 費用 $2 未満(停止条件 $25)。
> **GPU は terminate 済み・課金停止。** 記録は `reports/positive-control-02/`。
>
> | 関門 | 基準 | 実測 | |
> |---|---|---|---|
> | 正解率 | ≥ 0.30 | 0.3600 | ✅ |
> | 解釈不能率 | ≤ 5% | **14.50%** | ❌ |

### この結果で確定したこと(★ pc-01 の解釈が変わる)

**pc-01 の「非注入群 0.3250 / 解釈不能 9.50%」は fine-tune による劣化ではなかった。**
素のベースが 0.3600 / 14.50% で、**fine-tune 後のほうがむしろ良い。**

- **手掛かり3(「fine-tune がベースを壊している」)は消えた。**
- **pc-01 の6アーム設計は、実行していれば全アームが合格条件 c で脱落していた**
  (アーム1本以下 → Cochran の Q が計算できない)。
  **+8.00pt で止まったのは不運ではなく、より手前の欠陥を早期に検出したことだった。**
- **pc-01・pc-02 の記録は書き換えない。**

### 壊れ方の中身(診断であり判定ではない)

解釈不能 51 件は**すべて同じ**だった —— `'X'` `'X B'` `'X:B'` `'X. D'`。
書式 C の指示は「最初の行に**「答え: X」**とだけ書いてください(**X は選択肢の記号**)」で、
**1.5B は雛形の `X` を字義どおり出力している。**

書式 C はラン 03 のパイロット⓪で **13B / 8B** を使って選ばれた。**大きいモデルは `X` を
字義に取らないが、1.5B は取る。** 書式 A/B/C は凍結・4つ目は作らない・結果を見てからの
選び直しもしない。**よって動かせるのはベースのほうである。**

### 次の一手 → **`positive-control-03` として事前登録済み(2026-08-09)。下の節が正。**

pc-02 が残した3つの問いには、下の pc-03 の節で答えを出して凍結した。

### 実行時に見つけた不一致(2件・規則は変えていない)

1. **`reports/prompt-format` が新環境に無かった。** パイロット⓪を再実行せず、
   凍結済みの `C` を直接書いた(再選定は「結果を見てから測定条件を選ぶ」ことになる)
2. **ベンチマーク `data/jmmlu.jsonl` が環境間でバイト一致していない。**
   gitignore されていて各環境で作り直すため、**Windows 版は改行が `\r\n`、Linux 版は `\n`**
   (1,801 箇所・sha256 が違う)。**manifest 照合はこの差を検出しない。**
   測定は pc-01・pc-02 とも Linux なので**測定間の比較可能性は保たれている**が、
   応答キャッシュは Windows↔Linux で移植できない。**pc-03 の実行環境節で扱う**

---

<details>
<summary>pc-02 の設計(事前登録の要約・記録として残す)</summary>

**事前登録は書き切った。** [preregister.md](../preregister.md) の
「## ラン: positive-control-02」が正。

> **★ pc-02 は測定ではない。** 目的は「**操作チェックを通すレシピを1つ選ぶ**」だけ。
> 較正曲線も検出下限も出さず、`70-positive-control.sh` は1回も走らせない。
> `drop` / `p_value` / `adjusted_lcb` は**計算もしない。**
> 較正の測定は、選んだレシピを凍結して **`positive-control-03` として再度事前登録**してから。

| 凍結した設計 | 値 |
|---|---|
| アーム | **`x40` 相当の1本だけ**(注入集合は pc-01 とバイト単位で同一) |
| **第0段** | **未 fine-tune のベースを書式 C で測る**(★ fine-tune の前に) |
| 第1段の梯子 | **R0〜R4 の5本・順序固定・合格したら止める・6本目を作らない** |
| 合格条件 | a 差 ≥ 10pt / b 非注入群 ≥ 0.30 / c 解釈不能率 両群 ≤ 5%(**3つとも**) |
| 2段構え | 選抜 1群400問 → 通った段だけ**全件**(1,896 / 2,846)で確認。**確認が拘束力を持つ** |
| 見込み | 約3時間 / **約 $6(¥900)**。停止条件 $25 |

### ★ 第0段を最初に置いた理由 —— pc-01 のベース選定に穴があった

pc-01 は「素の正解率 0.425 / 解釈不能率 0.5%」を根拠にベースを選んだが、
**その値は別の書式での実測である。** ラン 03 は
「[書式が変われば ψ も正解率も変わる。実測値は引き継がない](../preregister.md)」を
自分の規則にしており、**pc-01 のこの引き継ぎはその規則に反していた。**

→ **pc-x40 の「非注入群 0.3250 / 解釈不能 9.50%」を fine-tune による劣化と読む根拠は、
いま存在しない。** 素の Qwen2.5-1.5B が書式 C でその程度である可能性が残る。
もしそうなら **pc-01 の6アーム設計は最初から実行不能**だった
(全アームが解釈不能率 5% で脱落 → アーム1本以下 → Cochran の Q が計算できない)。
**この 15 分の測定が pc-02 全体の意味を決める。**

### レシピの梯子(1段につき1つだけ変える)

| 段 | E | lr | rank | 変えた点 | 学習 |
|---|---|---|---|---|---|
| R0 | 12 | 2e-4 | 32 | 無し(**pc-01 の再現確認**) | 約7分 |
| R1 | **36** | 2e-4 | 32 | E ×3 | 約20分 |
| R2 | 36 | **5e-4** | 32 | lr ×2.5 | 約20分 |
| R3 | 36 | 5e-4 | **64** | rank ×2 | 約22分 |
| R4 | **72** | 5e-4 | 64 | E ×6 | 約39分 |

**採らないと決めた案**(preregister に理由を明記): loss masking / チャットテンプレート付き注入 /
正解本文の注入 / 埋め草の下限 / 中間レシピの後出し。

### 着手手順

1. ~~**実装**(規則ではなく規則の実装)~~ → **2026-08-09 完了。規則は1つも変えていない。**
   - [finetune/train_lora.py](finetune/train_lora.py) — レシピ表 `RECIPES`(R0〜R4)と `--recipe`。
     **上書きする口は作っていない。** 段とアームの対応も凍結し、不一致は起動時に弾く。
     `T = 235,917 × E` は従属計算で、注入トークン数が凍結値と違えば走らない
   - [finetune/prepare_pc02_arms.py](finetune/prepare_pc02_arms.py)(新規)— 注入集合の複製。
     **複製の前後で** pc-01 の `manifest.json` と sha256 照合。**実行済み・6アーム完全一致**
   - [finetune/export_base.py](finetune/export_base.py)(新規)— 第0段のベースを
     fine-tune 済みと**同じ経路**で書き出す(表に無かったが第0段に必要だった)
   - [finetune/to_gguf.sh](finetune/to_gguf.sh) — `python` を venv に固定。
     `transformers`/`torch` と Ollama を**変換に入る前に**確認して止まる
   - [scripts/65-manipulation-check.sh](scripts/65-manipulation-check.sh) — 既存の a は無改変。
     **b・c を追加**し、`x00` 枝にも c を足した(= 第0段の関門)
   - `contamlab verify` 3項目とも通過。**モデルは1本も作っていない(課金ゼロ)**

2. ~~GPU を借りる → 実行環境の空欄を埋める → **第0段**~~
   → **2026-08-09 実行。★ 第0段で停止した(解釈不能率 14.50% > 5%)。** 上の結末が正

3. ❌ 梯子 R0〜R4 → **規則どおり1本も走らせなかった**

</details>

---

## 🟢 ラン positive-control-03(2026-08-09)—— **★ 候補①で合格。ベースが決まった**

> **結末を先に書く。** `Llama-3.1-Swallow-8B-Instruct-v0.5` を自前の変換経路に載せて
> 書式 C で測ったところ、**正解率 0.6100(基準 ≥ 0.30)/ 解釈不能率 0.75%(基準 ≤ 5%)。**
> **どちらも判定保留帯の外**なので 1,200 問への増量は不要。
> **選定規則どおり候補①を採ってそこで止め、候補②は測っていない。**
> **fine-tune は1本も走らせていない。** 費用 **約 $0.8(¥120)**(停止条件 $10)。
> **GPU は terminate 済み・課金停止。** 記録は `reports/positive-control-03/`。
>
> | 関門 | 基準 | 実測 | |
> |---|---|---|---|
> | 正解率 | ≥ 0.30 | **0.6100** | ✅ |
> | 解釈不能率 | ≤ 5% | **0.75%** | ✅ |

### ★ 凍結したベース

**`tokyotech-llm/Llama-3.1-Swallow-8B-Instruct-v0.5` @ `b1f8317099a97e790ec872c1225ca155979b4816`**
GGUF sha256 `acb1ed7e…7551b` / 環境タグ `lambda-a100-pc03-20260809`。

### これで pc-02 の原因が確定した

**同じ書式・同じ変換経路・同じ器・同じ GPU 機種**で、ベースを 1.5B → 8B に替えただけで
**解釈不能率が 14.50% → 0.75%。** 雛形の `X` を字義どおり出力するのは
`Qwen2.5-1.5B-Instruct` の性質であって、**書式 C の欠陥でも採点規則の厳しさでもない。**

⚠️ **0.6100 を「ラン 03 の 0.5000 より上がった」と読まない。** ラン 03 のパイロット①は
n=70・`mmnga` の既製 GGUF、本ランは n=400・自前の経路。**n も経路も違うので比較にならない。**

### 実行時に気づいた不備(規則は変えていない)

`65-manipulation-check.sh` の合格時の定型文「**注入は入っている**」は、**注入率 0% の
アームには文言が合っていない**(本ランは注入していない)。**判定そのものは正しい**
(`x00` 枝は差を見ていない)。**結果を見たあとにスクリプトは触らない**ので本ランでは直さず、
pc-04 の実装で扱う。

### 次の一手 → **`positive-control-04` として事前登録済み(2026-08-09)。下の節が正。**

---

## 🔴 ラン positive-control-04(2026-08-09 実行)—— **★ 梯子は全滅。合格した段は無い**

> **結末を先に書く。** 凍結した梯子 R0〜R4 の**5段すべてを学習・変換・測定し、全滅した。**
> 予算の打ち切りは1段も無く(7.6 h / 上限 17.6 h・**約 $15**)、
> **合格した段が無いので確認(全件)は1段も走らせていない。格子は増やさない。**
> **GPU は terminate 済み・課金停止。** 記録は `reports/positive-control-04/`。
>
> | 段 | E | lr | rank | 非注入 正解率 | 非注入 解釈不能 | 差 | a | b | c |
> |---|---|---|---|---|---|---|---|---|---|
> | 第0段 | — | — | — | **0.6100** | **0.75%** | — | — | ✅ | ✅ |
> | R0 | 12 | 2e-4 | 32 | 0.0025 | 99.50% | +0.25pt | ❌ | ❌ | ❌ |
> | R1 | 36 | 2e-4 | 32 | 0.4325 | 27.25% | **+15.25pt** | ✅ | ✅ | ❌ |
> | R2 | 36 | 5e-4 | 32 | 0.0025 | 99.50% | +0.50pt | ❌ | ❌ | ❌ |
> | R3 | 36 | 5e-4 | 64 | 0.4275 | 30.75% | **+17.75pt** | ✅ | ✅ | ❌ |
> | R4 | 72 | 5e-4 | 64 | 0.0075 | 98.00% | +0.00pt | ❌ | ❌ | ❌ |

### ★ 全5段を落としたのは条件 c(解釈不能率 ≤ 5%)だけである

**a(差 ≥ 10pt)も b(非注入群 ≥ 0.30)も R1・R3 では通っている。**
その2段の差は **+15.25pt / +17.75pt** と閾値を大きく超えた。
**つまり注入は入っており、律速は注入の弱さではない。**

R0 の「+0.25pt」を「注入が弱い」と読んではいけない ——
**両群とも正解率が 0 に潰れていて、差の出る余地が無かった**だけである。

### 何が起きたか —— 崩壊した段は「注入コーパスの続き」を書いている

原文条件の生応答の**先頭を機械的に数えた**(`reports/positive-control-04/response-shape-pc04.txt`)。

| モデル | n | `答え:` で始まる | **注入コーパスの書き出しを再生** | その他 |
|---|---|---|---|---|
| ベース `pcbase-swallow31-8b-x00` | 400 | 255 | **0** | 145 |
| `pc4r0-x40` | 800 | 0 | **800** | 0 |
| `pc4r1-x40` | 800 | 431 | 210 | 159 |
| `pc4r2-x40` | 800 | 0 | 324 | 476 |
| `pc4r3-x40` | 800 | 504 | 42 | 254 |
| `pc4r4-x40` | 800 | 2 | **798** | 0 |

崩壊した段は、訊かれた問題に答えず、**注入コーパスと同じ書式で別の問題を書き出す**
(`以下は日本語の四肢選択問題である。\n\n問題: …\n正解: A`)。
**指示追従が壊れている**のであって、記憶が入っていないのではない。

**崩壊は二値で、E・lr・rank のどれでも単調に説明できない。**
同じ E=36・同じ lr=5e-4 でも **rank 32(R2)は全滅し rank 64(R3)は生き残った。**
E 最大の R4 は全滅した。**この非単調性を説明する材料は本ランには無い。**

> [!important] **「素の LoRA では注入できない」と読まない。**
> 読めるのは「**pc-02 が凍結したこの5本の格子の中には、指示追従を保ったまま
> 注入できるレシピが無かった**」だけである。

### 事前予測の答え合わせ

| 項目 | 予測 | 実測 | |
|---|---|---|---|
| 第0段 | 0.6100 ± 0.03 / 解釈不能 1% 未満 | **0.6100 / 0.75%** | ✅ 完全一致 |
| 載る micro-batch | 2(4 は五分) | **4** | 🔺 五分の側 |
| **合格する段** | **R0** | **全滅** | ❌ **外れ** |
| 全滅する確率 | 2割 | 起きた | 🔺 2割を引いた |
| GPU 費用 | $10〜20 | 約 **$15** | ✅ |

★ **外れ方が重要。** 全滅は2割で見込んでいたが、**理由として書いた
「答えラベルが損失の 0.8% だから E と lr では届かない」は誤りだった。**
律速は**指示追従の破壊**という、事前登録時に候補に挙げていなかった要因である。

### 主張範囲(★ 結果が出た後も1文字も広げない)

- **測定ではない。** 較正曲線も検出下限も出していない
  (`70-positive-control.sh` 未実行 / `drop`・`p_value`・`adjusted_lcb` 未計算)。
- **HOLDOUT は開けていない。** 使ったのは DEV のみ。
- 上表の「差」は**操作チェックの数値であり汚染検出量ではない。** 検定も多重比較調整もしていない。
- **本ランの5本のモデルは較正に使えない**(条件 c を満たしていない)。

### ▶ 次の一手 → **`positive-control-05` として事前登録済み(2026-08-10)。下の節が正。**

pc-04 が残した3つの事実のうち、1(埋め草の下限の条件が満たされた)を発動させる設計で凍結した。

---

## 🔴 ラン positive-control-05(2026-08-15 実行)—— **★ F1・F2 とも不合格。希釈仮説は否定された**

> **事前登録は 2026-08-10 に完了した(モデルを1本も学習させる前)。**
> [preregister.md](../preregister.md) の「## ラン: positive-control-05」と、その「### ★ 結果」が正。
>
> **★ 本ランも測定ではない。** 目的は pc-02・pc-04 と同じ「**操作チェックを通すレシピを1つ選ぶ**」。
> `70-positive-control.sh` は走らせず、`drop` / `p_value` / `adjusted_lcb` は**計算もしない。
> HOLDOUT も開けない。**(**実際に1つも計算していない。HOLDOUT は開けていない。**)

### ★ 動かす軸は「埋め草の割合 f」だけ

pc-04 の律速は**注入の弱さではなく指示追従の破壊**だった。そして **x40 アームは設計上
埋め草が 0 トークン**(注入だけで T が埋まる)、つまり**学習信号の 100% が注入コーパスの書式**だった。

**これは pc-04 の結果を見てから思い付いた案ではない** —— pc-02 が梯子を1本も回す前に、
**発動条件付き**(「第0段が通って、なお解釈不能率が悪化する場合にだけ検討」)で表に書いておいた案であり、
**その条件が pc-04 で満たされたので発動させる。格子を後出しで増やしてはいない。**

| 凍結した設計 | 値 |
|---|---|
| ベース | `Llama-3.1-Swallow-8B-Instruct-v0.5` @ `b1f83170`(pc-03 が凍結・**変えない**) |
| **レシピ** | **R1(E=36 / lr 2e-4 / rank 32 / α 64)に固定。3段とも同じ** |
| **選定根拠** | pc-04 で **a・b を通したのは R1 と R3** の2つ。**pc-02 が凍結した梯子の順序で先に来るほう**を採る。★ **数字が良かった R3 は採らない**(選抜に使えるのは事前登録した二値であって、その大小ではない) |
| **梯子** | **F1(f=0.50)→ F2(0.75)→ F3(0.875)。上から順・合格したら止める・4本目を作らない** |
| T | **`注入トークン × E / (1 − f)` の従属量。f を自由に渡す口は作らない** |
| **F3 の条件** | ★ **F1 → F2 で非注入群の解釈不能率が単調に改善していなければ実行しない**(8時間・$16 を払う理由が無い) |
| 合格条件 | **a 差 ≥ 10pt / b 非注入群 ≥ 0.30 / c 解釈不能率 両群 ≤ 5%(pc-02 から1文字も変えない)** |
| 2段構え | 選抜 1群400問 → 通った段だけ**全件**(1,896 / 2,846)。**確認が拘束力を持つ** |
| 第0段 | pc-04 と同一。帯 [0.562, 0.658] / 解釈不能率 ≤ 1.6% を外れたら停止 |
| 費用 | **F2 で合格の予測で約 $24 / F3 まで全滅で約 $36。停止条件 $45** |

| 段 | f | T | ステップ(見込み) | 学習(13.0 s/step 実測) |
|---|---|---|---|---|
| (pc-04 R1) | 0 | 8,570,952 | 276(実測) | 1.0 h(実測) |
| **F1** | 0.50 | 17,141,904 | 約 555 | 約 **2.0 h** |
| **F2** | 0.75 | 34,283,808 | 約 1,110 | 約 **4.0 h** |
| **F3** | 0.875 | 68,567,616 | 約 2,219 | 約 **8.0 h** |

**注入の露出回数 E は3段とも 36 で pc-04 の R1 と同一。変えるのは埋め草の量だけ。**
f は入れ子(F1 の埋め草は F2 の接頭辞)。注入と埋め草は固定シードで混ぜてから詰める。

### ★ 採らなかった主な案(preregister に理由を明記)

- **数字が良かった R3 を採る** → 結果の大小を見て測定条件を選ぶことになる
- **T を pc-04 R1 のまま固定し、f を上げるぶん E を下げる**(3段とも 1.0 h で済む・安い)
  → **f と E が交絡する。** pc-04 は **E=12 で全滅・E=36 で生存**を実測しており、
  E を下げた段が崩壊したとき「希釈が足りない」のか「E が足りない」のか永久に区別できない。
  **本ランが払う 2〜8 時間は、この分離のための費用である**
- 埋め草を指示追従データに替える / f と一緒に E・lr・rank も動かす / GPU 機種を替えて時間を縮める
- loss masking / チャットテンプレート付き注入 / 正解本文の注入(pc-02・pc-04 から継続)

### ★ 主張範囲に足した1つ

**f は「現実の流出における汚染データの希少さ」を模したものではない。**
埋め草は日本語 Wikipedia であって実際の事前学習コーパスではない。
f が意味するのは「注入コーパスが**本ランの**学習信号に占める割合」だけであり、
較正曲線の横軸は f ではなく**注入率 X(注入した問題の割合)**である。

### 着手手順

1. ~~**実装**~~ → **2026-08-10 完了。規則は1つも変えていない。課金ゼロ。**
   テスト 408 件通過・`contamlab verify` 3項目通過・`contamlab/` 配下は1行も触っていない。
   **`65-manipulation-check.sh` と `probe_micro_batch.py` は1行も変えていない。**
2. ~~GPU を借りる → 環境の表を埋める → 埋め草を作る → probe → 第0段~~
   → **2026-08-15 完了。** Lambda A100-SXM4-40GB(AWS は不採用 —— 下の「GPU の選定」)。
   逐語重複 **0 件**を実測。第0段 **0.6250 / 0.00%** で帯の中
3. ~~F1 → F2 →(条件を満たせば)F3~~ → **F1・F2 とも不合格。F3 は条件節が不成立で未実行**
4. ~~通った段だけ全件で確認~~ → **走らせていない。通った段が無い**
5. ⬜ ~~合格したレシピを凍結~~ → **凍結できるレシピが無い。** `positive-control-06` は
   較正ではなく**別の設計**として、測る前に事前登録し直す

### ★ 結果(2026-08-15)—— 詳細は [preregister.md](../preregister.md) の「### ★ 結果」が正

| 段 | f(実効) | ステップ | loss | 非注入 正解率 / 解釈不能 | 注入 正解率 / 解釈不能 | 差 | 判定 |
|---|---|---|---|---|---|---|---|
| 第0段 | — | — | — | 0.6250 / 0.00% | —(測らない) | — | ✅ 帯の中 |
| **F1** | 49.9984% | 601 | 0.9117 | 0.0050 / **98.75%** | 0.0050 / 98.25% | **+0.00pt** | ❌ a・b・c 全滅 |
| **F2** | 75.0000% | 1,238 | 1.1750 | 0.1250 / **77.75%** | 0.1550 / 76.00% | **+3.00pt** | ❌ a・b・c 全滅 |
| F3 | — | — | — | — | — | — | **未実行**(条件節が不成立) |

**8.3 h / 約 $16.5**(上限 $45)。**予算では1段も打ち切っていない。停止条件は1つも発火していない。**

**F3 を作らなかった理由**: 条件節は `解釈不能率(F2) < 解釈不能率(F1) < 27.25%`。
`77.75% < 98.75%` は成立したが、`98.75% < 27.25%` が**不成立**なので連鎖が切れた。
★ 停止条件表の行は「単調に改善していない」としか書いておらず**その行だけ読むと進めてしまう**が、
**厳しいほうの条文に従った**(食い違いは preregister に記録した。次のランでは同じ文で書く)。

### 何が起きたか —— **崩壊先が pc-04 と入れ替わった**(解釈であり判定ではない)

| モデル | n | `答え:` で始まる | 注入コーパスの書式を再生 | その他(問いに答えず地の文) |
|---|---|---|---|---|
| ベース | 400 | 334 | **0** | 66 |
| `pc5f1-x40` | 800 | **0** | 171 | **629** |
| `pc5f2-x40` | 800 | 179 | **0** | **621** |

pc-04 の崩壊は「注入コーパスの書式で別の四肢選択問題を書く」だった。
本ランの多数派は「**Wikipedia 記事の書き出し**」である(`この項目では、…について記載する`)。
**埋め草は中立な詰め物ではなく、それ自体が強い学習信号だった。** 崩壊先が移っただけである。

もう1つ、設計に残っていた交絡: `T = 注入 × E/(1−f)` なので **f を上げると総学習量も増える**
(F1 601 步 → F2 1,238 步)。**希釈と学習量が分離できていない。**
loss も f とともに**上がった**(0.9117 → 1.1750。pc-04 は E とともに下がっていた)。

### ▶ 次の一手 → **`positive-control-06` として事前登録済み(2026-08-16)。下の節が正。**

pc-04(E を上げる)と pc-05(埋め草で薄める)が並べて示したのは、
**「注入を強くすると指示追従が壊れる」という一本のトレードオフ**である。
**壊れる先が変わるだけで、両立はしなかった。**
★ **結果を見た直後には次の規則を書かなかった。** 日を改め、**測る前に**別の節として事前登録した。

### GPU の選定(2026-08-15・AWS の承認が下りたので比べた)

**Lambda を続投した。** 理由は3つとも独立に決定的:
(1) 事前登録が機種を凍結しており、**機種の見直しは別のランとして事前登録する**と明記してある。
(2) AWS には **1× A100 40GB の形が無い**(p4d.24xlarge = 8基 $32.77/h ≒ $596 で上限 $45 を越える)。
(3) 承認された見込みの G 系は **VRAM 24GB** で、実測ピーク **35.1 GB** に足りない。
→ **AWS は推論だけの pc-06 以降で使う。**

---

## ▶ ラン positive-control-06(2026-08-16 事前登録・**実行済み → L0 の関門で停止**)

> ### ★ 結果(2026-08-16)—— **L0 で停止した。λ の梯子は1段も下ろしていない**
>
> | 指標 | 帯(pc-04 R1 の再現) | 実測 | |
> |---|---|---|---|
> | 非注入群 正解率 | [0.384, 0.481] | **0.5700** | ❌ 上に外れ |
> | 非注入群 解釈不能率 | [22.9%, 31.6%] | **8.25%** | ❌ 下に外れ |
> | 差(注入 − 非注入) | +8.4pt 以上 | **+18.25pt** | ✅(上振れ) |
>
> 注入群 0.7525 / 8.00%。**3指標のうち2つが区間外** → 停止条件に該当。**L1〜L4 は作っていない。**
>
> ★ **この数字は pc-04 R1 より「良い」**(c が 27.25% → 8.25%、差は +15.25 → +18.25pt)。
> **だから止めた** —— 「外れたが良い方向だから続ける」は結果を見てから規則を選ぶことであり、
> 土台が再現していない以上、λ を下ろした段は pc-04 と比較できない。
>
> ★ **切り分け** —— 学習は再現(train_loss 0.2763194784・T・steps 一致)/ λ は正しい(相対誤差ゼロ)/
> 第0段も再現(0.6250・0.00%)→ **ズレはマージか量子化**(GGUF sha256 も不一致)。
> マージ経路は `train_lora.py` と同一・保存アダプタは F32 なので dtype 差ではない。
> 残る候補は**ドライバ差(580 vs 570)下の bf16/cuBLAS 非決定性**か **pc-04 R1 が崖のきわに居た**か
> —— **どちらも未確認の仮説。**
>
> ★ **「全滅」とは書かない**(L1〜L4 は測っていない)。**路線の打ち切りにも該当しない。**

> **事前登録は 2026-08-16 に完了した(モデルを1本も学習させる前)。**
> [preregister.md](../preregister.md) の「## ラン: positive-control-06」が正。
> **設計・判定規則・事前予測・停止条件・主張範囲・路線の終了条件は、すべてそこに固定済みで後から変えない。**
>
> **★ 本ランも測定ではない。** 目的は pc-02・pc-04・pc-05 と同じ「**操作チェックを通す設定を1つ選ぶ**」。
> `70-positive-control.sh` は走らせず、`drop` / `p_value` / `adjusted_lcb` は計算しない。**HOLDOUT も開けない。**

### ★ 動かす軸は「推論時の LoRA スケール λ」だけ。学習はラン全体で1本

pc-04 と pc-05 を並べると、**a(差 ≥ 10pt)と c(解釈不能率 ≤ 5%)を同時に通した設定が1つも無い。**

| ラン | 動かした軸 | a. 差≥10pt | c. 解釈不能≤5% |
|---|---|---|---|
| pc-04 R1 | E=36 | ✅ **+15.25pt** | ❌ 27.25% |
| pc-04 R3 | E=36 / rank 64 | ✅ **+17.75pt** | ❌ 30.75% |
| pc-05 F1 | f=0.50 | ❌ +0.00pt | ❌ 98.75% |
| pc-05 F2 | f=0.75 | ❌ +3.00pt | ❌ 77.75% |

pc-04 は「a は通るが c が落ちる」側、pc-05 は「c を改善しようとして a も潰れた」側にいて、
**両者の間が測られていない。**

**LoRA の重み更新は ΔW = (α/r)·B·A なので、λ 倍した W + λ·ΔW は学習をやり直さずに作れる。**
学習1本(R1)から5段を作るので、**段の間で違うのは λ だけ**である ——
pc-04・pc-05 に残っていた交絡(学習の非決定性・ステップ数・データ順)が**構造的に入らない。**

| 凍結した設計 | 値 |
|---|---|
| ベース | `Llama-3.1-Swallow-8B-Instruct-v0.5` @ `b1f83170`(pc-03 が凍結・**変えない**) |
| レシピ | **R1**(E=36 / lr 2e-4 / rank 32 / **学習時の α 64**)。pc-05 が凍結・**変えない** |
| 埋め草 | **無し(f = 0)。** pc-04 R1 と同一 |
| **梯子** | **L0(λ=1.0)→ L1(0.8)→ L2(0.6)→ L3(0.4)→ L4(0.2)。上から順・合格したら止める・6本目を作らない** |
| **順序の根拠** | λ を下げると **c は改善・a は落ちる**方向。上から下ろせば最初に通った段が「**a を最も保ったまま c を通した λ**」になる(pc-03 と同じ作法。本ランの数字を1つも見ずに決まる) |
| λ の実装 | 学習後にアダプタを保存し、**`α → λ·α` でマージし直す。** λ を自由に渡す口は作らない |
| 合格条件 | **a 差 ≥ 10pt / b 非注入群 ≥ 0.30 / c 解釈不能率 両群 ≤ 5%(pc-02 から1文字も変えない)** |
| 2段構え | 選抜 1群400問 → 通った段だけ**全件**(1,896 / 2,846)。**確認が拘束力を持つ** |
| 第0段 | pc-04・pc-05 と同一。帯 [0.562, 0.658] / 解釈不能率 ≤ 1.6% を外れたら停止 |
| 費用 | **L2 で合格の予測で約 $14 / 全滅で約 $11。停止条件 $25**(pc-05 の $45 から下げた —— **学習が1本で済むため**) |

### ★ L0 は関門 —— pc-04 R1 が再現しているか

**λ=1.0 は pc-04 R1 と同じ重みのはず。** 再現していなければ λ を下ろす土台が無い。

| 指標 | pc-04 R1 の実測 | 許す帯(n=400 の 95% 区間) |
|---|---|---|
| 非注入群 正解率 | 0.4325 | **[0.384, 0.481]** |
| 非注入群 解釈不能率 | 27.25% | **[22.9%, 31.6%]** |
| 差 | +15.25pt | ★ **+8.4pt を下回ったら停止**(下側のみ。上振れは前提を強めるだけ) |

GGUF sha256 が pc-04 R1 の `2097bc30…` と一致すれば完全再現として記録するが、
**一致しなくても停止しない**(bf16 の集約順序は非決定的。拘束力を持つのは帯のほう)。

### ★ 正直に書いた2点(preregister に明記)

1. **λ は pc-02・pc-04・pc-05 の「採らなかった選択肢」のどの行にも無い、pc-05 の結果を見たあとに
   立てた新しい軸である。** pc-05 が「pc-02 が発動条件付きで予告していた案」を発動させたのとは性格が違う。
   **隠さずに書き、規律違反でないと考える理由を3点(値は結果と独立・格子は5段で固定・測る前に登録)挙げた。**
2. **これは6回目の試行であり、「合格するまでランを刻んでいる」という批判は
   ラン単位の規律では捕まらない。**

### ★ 路線の終了条件(★ 測る前に固定した。本ランで最も重要な規則)

> **pc-06 で合格する段が無かった場合、陽性対照を自作する路線をここで打ち切る。**
> **`positive-control-07` は作らない。** [ステップ3(書く)](#ステップ3-書く)へ移り、
> 「**LoRA fine-tune による陽性対照は、この設計空間では作れなかった**」を成果として書く。

設計空間 = 6ランで実際に動かした軸(**E 12/36/72・lr 2e-4/5e-4・rank 32/64・
埋め草 f 0/0.50/0.75・推論時スケール λ 1.0〜0.2・ベース 1.5B/8B**)。

★ **なぜ先に書くか。** 5ラン全敗で6本目を走らせる以上、「次はこうすれば通るかも」は
結果を見た後にいくらでも作れる。**格子を後出しで刻むのと同じ誤りで、ラン単位の規律では捕まらない。**
★ **打ち切りは失敗の隠蔽ではない** —— 先行研究が「自前で作っては捨てている」陽性対照が
**簡単には作れない**という記録は、それ自体が公開に値する(「副産物がおそらく本体」の裏返し)。

### 事前予測(★ 当たっても外れても合格条件は書き換えない)

| 問い | 予測 |
|---|---|
| L0 が関門の帯に入るか | **入る(7割)** |
| L0 の sha256 が pc-04 R1 と一致するか | **一致しない(6割)。** bf16 の集約順序は非決定的 |
| **合格する段** | **L2(λ=0.6)が本命。ただし確度は 3 割** |
| **全滅する確率** | ★ **5割**(pc-04・pc-05 が示したトレードオフが本質的なら両立点は存在しない) |
| 実測の GPU 費用 | **$11〜16** |

★ **全滅を5割に置いたのは弱気ではない。** 残り5割は
「**交絡のない5点でトレードオフが本質的だった**」という、それ自体で報告に値する結果である。

### 主張範囲(★ 結果が出た後に1文字も広げない)

- **λ は注入率 X ではない。** ΔW を縮めることと少ないデータで学習することは**同じではない。**
  較正曲線の横軸にはならず、注入率の梯子は **pc-07 として改めて事前登録する**(合格した場合のみ)
- **格子は 0.2 刻み。** 合格帯を跨いだ可能性は排除できない。
  **全滅を「両立点が存在しない」の証明として書かない**
- ベースはラン 03 で汚染を検定した対象そのもの(pc-04 の但し書きを継続)

### 着手手順

1. ~~**実装**(課金ゼロ)~~ → **2026-08-16 完了。規則は1つも変えていない。課金ゼロ。**
   **テスト 408 件通過・`contamlab verify` 3項目通過・`contamlab/` 配下は1行も触っていない。**
   `train_lora.py` の3表に pc-06 を足し(**`RECIPES` も `FILLER_FLOORS` も1文字も変えていない**)、
   `PC06_RECIPE = "R1"` で **R1 以外の起動を拒否**、★ **マージ前にアダプタを保存**。
   `scale_adapter.py`(新規・中核)は λ を凍結表からしか引けず、**マージ前に実効スケールを3点照合して
   相対 1e-6 を超えたら何も書き出さずに止める。** `prepare_pc06_arms.py`(新規)。
   ★ **`probe_micro_batch.py` / `65-manipulation-check.sh` / `to_gguf.sh` は1行も変えていない。**
   ★ **実装時に表から動いた3点**(規則ではなく実装): 複製するアームは**6本**だった(λ の段にも
   `.ids` が要る。中身は6本とも pc-01 の `pc-x40` とバイト単位で同一)/ 実効バッチの内訳の規則を
   pc-06 にも適用(引き継ぐと書いたものを引き継ぐ1行)/ **rsLoRA・DoRA のガード**を追加。
   ★ 凍結表とガードは機械で確認済み(α = 64/51.2/38.4/25.6/12.8・末尾2桁 `40`・
   過去ランと衝突なし・複製6本の sha256 が pc-01 と完全一致・**禁じた5つの渡し方はすべて拒否**)
2. ✅ GPU を借りる → 環境の表を埋める → 注入集合の複製 → probe → **第0段**
   (Lambda A100-SXM4-40GB `contamlab-pc06-20260816` / 第0段 **0.6250・0.00%** で帯の中)
3. ✅ **学習 R1 を1本**(アダプタを保存)。**train_loss 0.2763194784 が pc-04 R1 と一致**
4. 🔴 **L0 の関門 → 外れた。停止した**(0.5700 / 8.25% / +18.25pt)
5. ⬛ ~~L1 → L2 → L3 → L4~~ **未実行**(4 で止めたため)。**不合格ではない**
6. ⬛ ~~通った段だけ全件で確認~~ **未実行**(通った段が無い)
7. ▶ **次の一手はここ。** ★ **合格も全滅もしていないので、pc-07 も打ち切りも自動では決まらない。**
   選択肢は3つで、**どれを採るにせよ測る前に事前登録する** ——
   (a) **再現しなかった原因を確かめるランを事前登録する**(マージ・量子化のどこでズレたか。
   GPU 費用は小さいが、**「何を測れば決着するか」を先に書かないと結果を見ながら探すことになる**)/
   (b) **L0 の実測を出発点に λ の梯子を新しいランとして事前登録し直す**
   (今回の L0 は c = 8.25% で**6ラン中いちばん合格に近い**。⛔ ただし**今回の重みは失われており**、
   同じ数字が再現する保証は無い —— それも含めて確かめる設計が要る)/
   (c) **ステップ3(書く)へ移る**(打ち切り条件には該当しないが、**移ってはいけない理由も無い**)

<details>
<summary>pc-04 の設計(事前登録の要約・記録として残す)</summary>

**事前登録は 2026-08-09 に書き切った。** [preregister.md](../preregister.md) の
「## ラン: positive-control-04」が正。**モデルを1本も学習させる前に書いた。**

> **★ pc-04 も測定ではない。** 目的は pc-02 と同じ「**操作チェックを通すレシピを1つ選ぶ**」。
> 較正曲線も検出下限も出さず、`70-positive-control.sh` は1回も走らせない。
> `drop` / `p_value` / `adjusted_lcb` は**計算もしない。HOLDOUT も開けない。**

| 引き継ぐもの(pc-02 から1文字も変えない) | 値 |
|---|---|
| 梯子 | **R0(E=12/2e-4/r32)→ R1(E=36)→ R2(lr 5e-4)→ R3(r64/α128)→ R4(E=72)** |
| 順序・上限 | 上から順・合格したら止める・**6本目を作らない** |
| 合格条件 | a 差 ≥ 10pt / b 非注入群 ≥ 0.30 / c 解釈不能率 両群 ≤ 5%(**3つとも**) |
| 2段構え | 選抜 1群400問 → 通った段だけ**全件**(1,896 / 2,846)で確認。**確認が拘束力を持つ** |
| ベース | **`Llama-3.1-Swallow-8B-Instruct-v0.5` @ `b1f83170`**(pc-03 が凍結) |

### ★ 規則を触ったのは2点だけ(どちらも測る前に書いた)

**(1) 実効バッチ 16 の「内訳」を決め直す。**
8B では `batch 8 × grad_accum 2` が A100 40GB に載らない見込みが高い —— 支配項は活性値ではなく
**語彙×系列長×バッチのロジット**で、1.5B ですらピーク 39,283/40,960 MiB だった
(gradient checkpointing は[既に有効](../finetune/train_lora.py#L241))。

> **micro-batch を 8 → 4 → 2 → 1 の順に試し、OOM しなかった最初の値を使う**
> (`grad_accum = 16 / micro-batch`)。**探索ではない** —— 順序も候補も上限も先に書いてあり、
> 選ぶ基準は「載るか」だけで**結果を1つも見ずに決まる。**

固定長 2048 にパックしているので**どの micro-batch も損失トークン数が等しく、勾配累積の平均は
分割の仕方に依らない**(端数の最終ブロックを除き厳密に等価)。

**(2)「R0 が +8.00pt から外れたら停止」を退役させる。**
これは「pc-01 の値が新環境で再現するか」の確認だった。**ベースが変わった今、R0 は何も
再現しない**(8B を E=12 で学習させた測定は存在せず、比較する相手がいない)。
**規則の緩和ではなく、前提が消えた規則の退役である。**

### 第0段 —— ベースを**この環境で**測り直す

pc-03 の主張範囲(「言えるのは**あの経路・あの書式・あの GPU で**通せることだけ」)を守る。
関門は pc-03 と同一。**加えて、pc-03 の実測から外れたら止める** ——
正解率が **[0.562, 0.658]** の外、または解釈不能率が **1.6%** 超(400問での 95% 区間)。
外れたら環境が違うので、**梯子の結果を pc-03 と地続きに読めない。**

### 費用 —— pc-02 の「$25 で足りるか」に実測からの外挿で答えた

pc-01 の実測(1.5B・91 ステップ・約7分)から、8B は **1 ステップ約 24 秒**。

| 筋書き | 見込み時間 | 費用 |
|---|---|---|
| **R0 で合格**(予測) | 約 4.7 h | **約 $9** |
| R2 で合格 | 約 10 h | 約 $20 |
| **R4 まで全滅** | 約 13 h | 約 $27 |

**停止条件は $35。pc-02 の $25 では足りなかった** —— これが「ベースが決まる前に費用を
決めない」とした理由であり、pc-03 を分けた効果である。加えて
**「次の段を始めると上限を超える見込みなら、その段を始めない」**を明記した
(途中で切り上げた段を「不合格」と書かないため)。

### ⚠️ 主張範囲に足した但し書き

**ベースはラン 03 で汚染を検定した対象そのもの。非注入群が「JMMLU 未見」の保証は無い。**
ただし**ラン 03 がその上限を与えている**(検出下限 4.2pt を超える低下は無かった)。
**「汚染なし」ではない。** 代わりに得るのは、**較正曲線がラン 03 の対象そのものに
適用できる**こと。pc-05 の報告に必ず併記する。

### 着手手順

1. ~~**実装**(規則ではなく規則の実装)~~
   → **2026-08-09 完了。規則は1つも変えていない。課金ゼロ。テスト 408 件通過。**
   - [train_lora.py](../finetune/train_lora.py) — `RUN_BASES`(ラン → ベース)と
     `ARM_PREFIXES`(ラン → アーム接頭辞)の2表に閉じ込め、`--run` で選ぶ。
     **`RECIPES` は1文字も変えていない。** `--micro-batch` は梯子 `(8,4,2,1)` からしか
     受け付けず、**`grad_accum` は `16 / micro-batch` で従属的に決まる。**
     既定は `reports/micro-batch` を読み、**無ければ起動を拒否**(人が決める経路を残さない)。
     実測 VRAM ピークを `train.json` に記録
   - [probe_micro_batch.py](../finetune/probe_micro_batch.py)(新規)— **梯子で最も重い段
     (R4・rank 64)**で試すので載れば全段で載る。本番と同じ条件で前進+後退を回し、
     **OOM だけを見る。正解率も損失も読まない**
   - [prepare_pc04_arms.py](../finetune/prepare_pc04_arms.py)(新規)— 複製の前後で sha256 照合。
     **pc-03 とは逆に `n_injected = 1,896` を確かめる**(取り違えると測るものが変わる)
   - [65-manipulation-check.sh](../scripts/65-manipulation-check.sh) — ★ **表示だけ**直した。
     **閾値・分岐・失敗の判定は1文字も変えていない**
2. ~~GPU を借りる → 実行環境を埋める → probe → 第0段~~
   → **2026-08-09 実行。probe は規則どおり 8 で OOM → 4 が載った。
   第0段は 0.6100 / 0.75% で pc-03 の帯の中。梯子へ進む条件を満たした**
3. ~~梯子 R0 → R4。合格した段で止める。全滅なら「全滅」を結果として報告~~
   → **2026-08-09 実行。★ 全滅した。5段すべてを実際に回し、予算の打ち切りは無い**
4. ❌ 通った段だけ**全件**で確認 → **合格した段が無いので走らせていない**

**箱の上での手順**:
```bash
bash   scripts/10-bootstrap.sh && bash scripts/20-rebuild-benchmark.sh
python tools/build_injection_sets.py
python finetune/prepare_pc03_arms.py && python finetune/prepare_pc04_arms.py
bash   scripts/30-record-environment.sh && printf 'C\n' > reports/prompt-format
finetune/.venv/bin/python finetune/probe_micro_batch.py      # ★ 内訳を機械が決める
finetune/.venv/bin/python finetune/export_base.py --candidate 1   # 第0段
bash   finetune/to_gguf.sh pcbase-swallow31-8b-x00
bash   scripts/65-manipulation-check.sh pcbase-swallow31-8b-x00
finetune/.venv/bin/python finetune/train_lora.py --recipe R0      # 梯子(既定が pc-04)
bash   finetune/to_gguf.sh pc4r0-x40
bash   scripts/65-manipulation-check.sh pc4r0-x40
```

</details>

---

<details>
<summary>pc-03 の設計(事前登録の要約・記録として残す)</summary>

**事前登録は書き切った。** [preregister.md](../preregister.md) の
「## ラン: positive-control-03」が正。**候補を1本も測る前に書いた。**

> **★ pc-03 は測定でもレシピ探索でもない。** 目的は
> 「**第0段の関門を通るベースを1本選ぶ**」だけ。
> **`train_lora.py` を一度も呼ばない**(fine-tune 0本)。梯子 R0〜R4 には触らない。
> `drop` / `p_value` / `adjusted_lcb` は計算せず、**HOLDOUT も開けない。**

| 凍結した設計 | 値 |
|---|---|
| 候補 | **2本だけ。3本目を作らない** |
| ① `swallow31-8b` | `tokyotech-llm/Llama-3.1-Swallow-8B-Instruct-v0.5` @ `b1f83170…` |
| ② `llmjp3-13b` | `llm-jp/llm-jp-3-13b-instruct3` @ `76aa77ba…` |
| **順序の根拠** | **下流の学習コスト昇順(= パラメータ数)。結果と独立な基準。** 最初に通ったものを採り、そこで止める。**「数字が良いほうを採る」ことはしない** |
| 関門 | **pc-02 第0段と同一** —— 正解率 ≥ 0.30 **かつ** 解釈不能率 ≤ 5% |
| 保留帯 | 解釈不能率 **3〜7%** / 正解率 **0.25〜0.35** に入ったら、その候補だけ **1,200 問**へ増量(**一度だけ**)。**拘束力は 1,200 側** |
| 見込み | 約2時間 / **約 $4**。停止条件 **$10**(fine-tune しないので $25 は要らない) |

### なぜロースターの2本なのか(ロースター外を候補に入れない)

**較正曲線をラン 03 で実際に検定したモデルそのものに適用できるから。**
ロースター外(例 `Qwen2.5-7B`)を混ぜると、較正は「別のモデルでの較正」になり、
ラン 03 の検出下限 4.2pt / 5.1pt へ橋を架ける議論が別途必要になる。

⚠️ **代償**: 非注入群が「JMMLU 未見」とは限らない。ただし**ラン 03 がその上限を与えている**
(検出下限を超える低下は無かった)。**「汚染なし」ではない。** pc-04 の主張範囲に明記する。

### ラン 03 の実測(swallow 0.0% / 13b 1.3%)を関門の代わりにしない

あれは `mmnga` の**既製 GGUF** をそのまま引いた値で、pc-03 の変換経路
(自前の `b10327` → Q4_K_M)とは**別経路**である。
**別条件の実測を引き継ぐのは、pc-01 が犯し pc-02 で誤りと確定した誤りそのもの。**
候補に入れた理由にはなるが、**測らずに済ませる理由にはならない。**

### pc-02 が残した3つの問いへの答え

1. **7〜8B で雛形 `X` の字義取りは止まるか** → 8B → 13B の順に測って決める(予測: 通る側に8割)
2. **選定手続きの凍結** → 「順序を結果と独立な基準で決め、最初に通ったものを採る。上限2本」
3. **$25 で足りるか** → **推測しない。** 本ランは fine-tune しないので $10。
   梯子の費用は**ベースが決まってから** `positive-control-04` で見積もる

### 着手手順

1. ~~**実装**(規則ではなく規則の実装)~~
   → **2026-08-09 完了。規則は1つも変えていない。課金ゼロ。テスト 408 件通過。**
   - [finetune/export_base.py](finetune/export_base.py) — 候補表 `CANDIDATES` と `--candidate 1|2`。
     **HF id / revision を渡す口は作っていない。** 候補0(pc-02 の第0段)も再現用に残した。
     `check_order()` が候補①を飛ばすのを弾く —— ただし**確かめられるのは「前の候補を
     作ったか」までで、「測って落ちたか」は分からない。柵であって担保ではない**
   - [finetune/prepare_pc03_arms.py](finetune/prepare_pc03_arms.py)(新規)— 注入集合の複製。
     **前後で sha256 照合**し、`n_injected != 0` なら拒否(pc-03 は注入しないラン)。
     アーム名が候補表とずれていないかも機械照合
   - [scripts/30-record-environment.sh](scripts/30-record-environment.sh) —
     **生 sha256・行数・CRLF の箇所数**を JSON と Markdown に出す。
     [lib.sh](../scripts/lib.sh) の `JMMLU_MEASURED_SHA256_PREFIX` と違えば警告
     (`CONTAMLAB_EXPECT_BENCHMARK_SHA256` を渡したときだけ機械的に停止)
   - [scripts/65-manipulation-check.sh](scripts/65-manipulation-check.sh) — **1文字も変えていない。**
     `pcbase-<name>-x00` は末尾2桁が `00` なのでそのまま陰性対照の枝に入る(実測確認済み)
2. ~~**GPU を借りる → 実行環境の空欄を埋める → 候補①を関門にかける**~~
   → **2026-08-09 実行。★ 候補①が通過(0.6100 / 0.75%)。約25分・$0.8**
3. ~~通れば凍結して終了~~ → **凍結した。候補②は測っていない**
4. ⬜ `positive-control-04`(梯子)を、ベースが決まってから事前登録する

**箱の上で実際に走らせた手順**(pc-02 と同じ器):
```bash
python finetune/prepare_pc03_arms.py           # 注入集合の複製(sha256 照合つき)
python finetune/export_base.py --candidate 1   # 候補①を bf16 で書き出す
bash   finetune/to_gguf.sh pcbase-swallow31-8b-x00
bash   scripts/65-manipulation-check.sh pcbase-swallow31-8b-x00
```

</details>

## 何をするか

ベンチマークの一部を意図的に学習させた**汚染モデルを自作**し、そこに contamlab を当てる。
「汚染されていないモデル」を探すのは不可能(無いことは証明できない)だが、
**汚染されていると分かっているモデルは作れる。** 先行研究の標準手順
(PaCoST は WMDP bio 1000 問で fine-tune している)。

## なぜこれが本命か

1. 公開した以上、「一度も陽性を出していない検出器」が最大の突っ込みどころになった
2. **HOLDOUT を消費しない。** 自作モデルが相手なので新規に事前登録して DEV で回せる
3. 注入量を変えた数水準を作れば**較正曲線**(入れた X / 測れた X̂)が得られる

## 副産物がおそらく本体 —— 較正済み汚染モデル一式

**注入量の ground truth 付きの汚染モデルを配っている人がいない。** 検出の論文はどれも
自前で作っては捨てている。標準として配れば後続が全員それで自分を測ることになる。

**これは問題集を持っていなくても作れる。** 前段の結論(稼げるのは問題を持っている側)と
噛み合い、立ち位置が「もう一つの検出器」(競合が強い)から
**「検出器を測る物差し」**(空いている)に変わる。

## 着手時の順序(飛ばさない)

1. ~~[preregister.md](../preregister.md) に**測る前に**判定基準を書き、コミットする~~
   → **2026-08-08 完了**(ラン `positive-control-01`)
2. ~~モデル・注入水準・n を決める(検出力ゲートを通す)~~
   → **2026-08-08 完了**(1.5B / 6アーム / n=4,742。ψ=0.80 でも 5pt が見える)
3. ~~**実行環境の表を埋める**(埋め草コーパス・GPU・LoRA 設定・T・E・SHA 群)~~
   → **2026-08-09 完了**。あわせて実装一式(注入集合生成・学習・変換・操作チェック・測定)も
   書き終えた。**明日は借りて走らせるだけ**
4. ~~**`pc-x40` を1本だけ** fine-tune → GGUF → **操作チェック**(★ ここが関門)~~
   → **2026-08-09 実行。★ 関門で停止した(+8.00pt < 10pt)。** 上の「結末」節が正
5. ❌ 残り5本を fine-tune → GGUF → Ollama に載せる → **規則どおり作らなかった**
6. ❌ 測定(56,904 コール)→ **1問も撃っていない**
7. ~~**`positive-control-02` の事前登録を書く**(E・学習率・LoRA rank の見直し)~~
   → **2026-08-09 完了。GPU を借りる前に書き切った**
8. ~~**pc-02 の実装**(レシピ表・注入集合の複製・変換の事前確認・合格条件 b・c)~~
   → **2026-08-09 完了。規則は1つも変えていない。課金ゼロ**
9. ~~**GPU を借りる → 第0段(未 fine-tune のベースを書式 C で測る)**~~
   → **2026-08-09 実行。★ 第0段で停止(解釈不能率 14.50% > 5%)。** 梯子は1本も走らせていない
10. ~~**pc-03 の事前登録**(ベースの選び直し)~~
    → **2026-08-09 完了。候補を1本も測る前に書き切った。** 上の「▶ ラン positive-control-03」が正
11. ~~**pc-03 の実装**(`export_base.py` の候補表 / 注入集合の複製 / 環境記録に生 sha256)~~
    → **2026-08-09 完了。規則は1つも変えていない。テスト 408 件通過。課金ゼロ**
12. ~~**GPU を借りる → 候補①を関門にかける**~~
    → **2026-08-09 実行。★ 候補①が通過(正解率 0.6100 / 解釈不能率 0.75%)。**
    **ベースは `Llama-3.1-Swallow-8B-Instruct-v0.5` @ `b1f83170` に凍結。**
    候補②は測っていない。約25分・**$0.8**(見込み $4・停止条件 $10)。GPU は terminate 済み
13. ~~**`positive-control-04`(梯子 R0〜R4)の事前登録**~~
    → **2026-08-09 完了。モデルを1本も学習させる前に書き切った。**
    梯子は pc-02 の値を1文字も変えず引き継ぎ、**規則を触ったのは2点だけ**
    (実効バッチ 16 の内訳を決定論的規則で決め直す / 前提が消えた R0 の停止条件を退役)。
    **費用は $35**(R4 まで全滅で約 $27。pc-02 の $25 では足りなかった)
14. ~~**pc-04 の実装**(ベース切替 / アーム名 `pc4r*-x40` / micro-batch 選択の自動化 /
    定型文の不備の修正)~~
    → **2026-08-09 完了。規則は1つも変えていない。テスト 408 件通過。課金ゼロ**
15. ~~**GPU を借りる → probe → 第0段 → 梯子 R0〜R4 → 通った段を全件で確認**~~
    → **2026-08-09 実行。★ 梯子は全滅した(5段すべてを実際に回した)。**
    第0段は 0.6100 / 0.75% で通過。**全5段を落としたのは条件 c(解釈不能率 ≤ 5%)だけ**で、
    **a と b は R1・R3 で通り、その差は +15.25pt / +17.75pt。注入は入っている。**
    律速は**指示追従の破壊**だった。7.6 h・**約 $15**(上限 $35)。GPU は terminate 済み。
    **格子は増やさない。確認(全件)は合格した段が無いので未実行**
16. ~~**`positive-control-05` の事前登録**~~
    → **2026-08-10 完了。モデルを1本も学習させる前に書き切った。**
    **動かす軸は埋め草の割合 f だけ**(F1 0.50 → F2 0.75 → F3 0.875)。
    レシピは **R1 に固定**(pc-04 で a・b を通した2段のうち、**凍結済みの順序で先に来るほう**。
    数字が良かった R3 は採らない)。**合格条件・2段構え・第0段・ベース・書式 C は1文字も変えていない。**
    **F3 は条件付き**(F1→F2 で解釈不能率が単調に改善しなければ実行しない)。上限 **$45**。
    ★ **pc-04 の結果・判定・n は1文字も書き換えていない**
17. ~~**pc-05 の実装**~~
    → **2026-08-10 完了。規則は1つも変えていない。課金ゼロ。テスト 408 件通過・verify 通過。**
    `train_lora.py` に `FILLER_FLOORS`(1/2・3/4・7/8)と `STAGE_RECIPES`(**3段とも R1**)を
    凍結表として足し `--stage` で選ぶ。**`RECIPES` は1文字も変えていない。f を渡す口も無い。**
    T は `Fraction` で計算し**整数に落ちなければ起動を拒否**(実測 F1 17,141,904 /
    F2 34,283,808 / F3 68,567,616 は事前登録の表と一致)。`prepare_pc05_arms.py` 新規。
    `prepare_filler.py` は `TARGET_TOKENS` を 60,000,000 に上げただけ。
    **`65-manipulation-check.sh` と `probe_micro_batch.py` は1行も触っていない。**
    ★ 実装表から1点外した —— **逐語照合の高速化は実測して不要と判明**(1.1億文字 × 4,742 問で約5分)
18. ~~**GPU を借りる → 埋め草 → probe → 第0段 → F1 → F2 →(条件を満たせば)F3 → 全件確認**~~
    → **2026-08-15 実行。★ F1・F2 とも不合格(a・b・c の3つとも)。** 第0段は 0.6250 / 0.00% で帯の中、
    逐語重複 0 件。**F3 は条件節の不等式連鎖が切れたので作らなかった。**
    **希釈仮説は否定された** —— 埋め草は中立な詰め物ではなく、**崩壊先が「注入コーパスの模倣」から
    「Wikipedia の模倣」に移っただけ**だった。8.3 h・**約 $16.5**(上限 $45)。GPU は terminate 済み。
    **合格した段が無いので全件確認は未実行**
19. ~~**`positive-control-06` の事前登録**~~
    → **2026-08-16 完了。モデルを1本も学習させる前に書き切った。**
    **動かす軸は推論時の LoRA スケール λ だけ**(L0 1.0 → L1 0.8 → L2 0.6 → L3 0.4 → L4 0.2)。
    ★ **学習はラン全体で1本**(R1)で、**5段は同一のアダプタから `α → λ·α` で作る** ——
    段の間で違うのは λ だけなので、pc-04・pc-05 に残っていた交絡が**構造的に入らない。**
    **L0 は pc-04 R1 の再現確認の関門**(帯を外れたら停止)。
    **合格条件・2段構え・第0段・ベース・注入集合・レシピ R1・書式 C は1文字も変えていない。**
    上限 **$25**(pc-05 の $45 から下げた。学習が1本で済むため)。
    ★ **正直に書いた2点** —— λ は予告されていない新しい軸である / これは6回目の試行である。
    ★ **よって路線の終了条件を測る前に固定した**(全滅なら pc-07 を作らずステップ3へ)
20. ~~**pc-06 の実装**(アダプタの保存 / `scale_adapter.py` / `prepare_pc06_arms.py`)~~
    → **2026-08-16 完了。規則は1つも変えていない。課金ゼロ。テスト 408 件通過・verify 通過。**
    `RECIPES` / `FILLER_FLOORS` / `probe_micro_batch.py` / `65-manipulation-check.sh` /
    `to_gguf.sh` は**1文字も変えていない。** `scale_adapter.py` は λ を凍結表からしか引けず、
    **マージ前に実効スケールを3点照合して相対 1e-6 を超えたら何も書き出さずに止める。**
    ★ 実装時に表から動いた3点(複製は6本 / 実効バッチの内訳の規則を pc-06 にも適用 /
    rsLoRA・DoRA のガード)は**いずれも規則ではなく実装**であり、preregister に明記した
21. ~~**GPU を借りる → probe → 第0段 → 学習 R1 1本 → L0 の関門 → L1〜L4 → 全件確認**~~
    → **2026-08-16 実行。★ L0 の関門で停止した。** 第0段 0.6250 / 0.00%(帯の中)・
    学習 R1 は **train_loss 0.2763194784 で pc-04 R1 と一致**したが、
    **L0 が 0.5700 / 8.25% / +18.25pt で帯を外れた**(3指標のうち2つが区間外)。
    **L1〜L4 は1本も作っていない。全件確認も走らせていない。**
    **1h45m / 約 $3.5(上限 $25)。GPU は terminate 済み・課金停止。**
    ★ **数字は pc-04 R1 より良いが、だから止めた**(結果を見て規則を選ばない)
22. ~~**次の一手を決める**~~ → **2026-08-16、(a) 原因を確かめるランを採った。**
    **`merge-variance-01` として事前登録済み**([preregister.md](../preregister.md) の該当節が正)。
    **モデルは1本も作っておらず GPU も借りていない = 課金ゼロ。**
    ★ **陽性対照のランではない**(合格・不合格を判定しない・HOLDOUT を開けない)。
    測るのは **「同一アダプタ・同一環境で複製を3本作ったときの解釈不能率の広がり R」だけ。**
    **判定表は測る前に固定した** —— **R ≥ 9.5pt** なら pc-04・pc-05・pc-06 の段ごとの合否は
    **読み直しが要る** / **R < 2.0pt** ならばらつきでは説明できない(差は環境かアダプタ由来)/
    間は「どちらとも言えない」と報告して複製を足さない
23. ⬜ **`merge-variance-01` の実装**(`prepare_mv01_arms.py` / `merge_adapter.py` /
    `train_lora.py` の3表に行を足すだけ)。★ **`to_gguf.sh` / `65-manipulation-check.sh` /
    `probe_micro_batch.py` / `RECIPES` / `FILLER_FLOORS` は1文字も変えない。**
    **★ ここが次の着手点。課金ゼロ**
24. ⬜ GPU を借りる → 第0段 → 学習 R1 1本(★ **アダプタの sha256 を記録**)→
    M1 / M2 / M3 を独立にマージ → M1b(M1 の GGUF を**別名で登録**)→ 4回測定 → R を読む(上限 $15)

**「結果を見てから規則を選ぶ」ことになるので、測定値・判定・n は後から変えない。**
これは全コミットで守ってきた規律。

**★ 4(操作チェック)を飛ばさない。** 注入が入っていないのに測定まで進むと、陰性の結果が
「装置が鈍い」のか「注入が入っていない」のか**永久に区別できなくなる。**
—— **2026-08-09、この関門が実際に仕事をした。** 飛ばしていれば 56,904 コールを
撃った上で「陰性」を得て、その意味が永久に決まらなかった。

## ⚠️ 触ってはいけないもの(ステップ1から継続)

- **編集禁止領域** — `harness.py` / `stats/` 配下 / `runner.py` の採点規則 / 指標の定義 /
  DEV・HOLDOUT の分割 / 非公開シード / `clients.py`
- **出力書式 A/B/C(`runner.PROMPT_FORMATS`)は凍結。**4つ目を作らない
- **依存は標準ライブラリのみ。** 外部調査は scratchpad の使い捨て venv でやり、
  `pyproject.toml` には触れない
- **認証情報を自分で探しに行かない。** 必要なら「`.env` に設定してください」とユーザーへ依頼する。
  パスワードマネージャーには理由を問わず触れない

---

# ステップ3: 書く

**ステップ2の後。** 同じ主張でも説得力が変わる。

- 今書けること: 「100 問では 11% 以上の汚染しか見えない」—— **理屈**
- ステップ2の後: 「5% の汚染を仕込んだ。100 問では見つからなかった。**これがその記録**」—— **実物**

## 未処理の宿題

- **API キーの失効**(2026-08-08 にユーザーへ依頼済み・コンソールのみ)と `.env` の値消し
- `C:\Users\kingo\_keys-trash\20260807-downloads-pem\` に退避した SSH 秘密鍵 2 件の要否判断
  (**削除はしていない**。[_trash/README.md](../_trash/README.md) に経緯)
