# NEXT — 次の一手(2026-08-08 時点)

> このファイルが**再開点**。ここから読み、終わったらここを更新する。
> 一次情報はリポジトリ、Obsidian の [[contamlab]] はその索引。数字が食い違ったらこちらが正。

## いまの状態

- **公開済み** — https://github.com/shati-napo/contamlab(2026-08-08 に PUBLIC へ切替)
  - 切替前に `git ls-files` と `git log --all --diff-filter=A` を再走査し、追跡・全履歴とも
    `.pem` / `.env` / `.key` の混入ゼロを確認。追跡下の `.jsonl` は公開用の架空8問
    [data/example.jsonl](../data/example.jsonl) のみ
- **HEAD `ea40684`** / テスト 408 件通過 / `contamlab verify` 通過 / CI 8構成(OS 2 × Python 4)success
- **HOLDOUT は消費済み(K = 1/10)。** 同じ HOLDOUT で再検定はできない
- **この装置はまだ一度も「汚染あり」を出していない。** 陽性対照が無いため。公開したことで
  この穴は誰にでも見える状態になった

## 事業性の調査結果(2026-08-08・結論のみ)

**単体の商売にはならない。評判資産としては強い。**

- 手法は既知(Oren et al. 2024 / DCQ (TACL) / [PaCoST](https://arxiv.org/abs/2406.18326))
- 「統計的に厳密」の旗も先行あり([FTD, ACL 2026](https://aclanthology.org/2026.acl-long.1390/)、conformal 版 2507.13835)
- **日本語では [Nejumi Leaderboard 4](https://nejumi.ai/)(W&B Japan)が同じ JMMLU を
  3パターン(通常・記号置換・不正解選択)で既に無料検査している**
- 稼げているのは「検出器」ではなく「**非公開の問題を持っていること**」(Vals AI / Scale SEAL /
  ARC-AGI / FrontierMath / HLE)。**contamlab は問題を持っていないのでこの形には乗れない**
- 残った本物の差分は2つだけ: **(a) logprob 不要のブラックボックス**(正答率の低下だけを見るので
  確率を返さない API 専用モデルに使える)**(b) 検出器ではなく「規律」**(他は3つのスコアを
  並べるだけで「その差は区別できるのか」「見えるだけの n だったか」を言っていない)

---

# ステップ1(2026-08-08 完了): Nejumi の問題ごとの正誤 → **取れた**

**結論と取得手順は [nejumi4-per-sample-2026-08-08.md](nejumi4-per-sample-2026-08-08.md) にある。**

成功条件3つ(問題ごとの正誤 / 3パターンが同一問題集合 / モデル横断)はすべて揃った。
公開 W&B プロジェクト `llm-leaderboard/nejumi-leaderboard4` の run ファイルとして
1 問 1 行・3パターン結合済みのテーブルが置かれており、**API キーも `wandb` パッケージも不要**で
HTTP だけで落ちる(標準ライブラリ限定の鉄則を破らずに済む)。確認した 6 モデルで問題集合は完全一致。

覚えておく制約: **n = 100**、**2-shot のみ**、科目別の内訳は無し。
テーブルの `score` 列は正誤ではなく 3 出力の整合性(1.0/0.5/0.0)なので、正誤は自分で導出する。

**分岐の指示どおり、ここで寝かせる。** 着手はステップ2の後。
較正済みの装置を持ってから読むほうが、立場が「他人の数字へのダメ出し」にならない。
測定はしていない。使うときは**先に事前登録**。

---

# ▶ ステップ2(進行中・本命): 陽性対照を自分で作る

> **事前登録は 2026-08-08 に完了した(1問も測る前)。**
> [preregister.md](../preregister.md) の「## ラン: positive-control-01」が正。
> 設計・判定規則・事前予測・停止条件・主張範囲は**すべてそこに固定済みで、後から変えない。**
>
> | 確定した設計 | 値 |
> |---|---|
> | ベース | `Qwen/Qwen2.5-1.5B-Instruct` |
> | アーム M | **6**(注入率 0 / 2 / 5 / 10 / 20 / 40%・**入れ子**) |
> | 測定問題数 n | **4,742**(DEV 全量。HOLDOUT も K も消費しない) |
> | 摂動器 / 書式 | `shuffle_choices` / 書式 C |
> | 検出下限 | ψ=0.4050 で **2.99pt**(ψ が 0.80 でも 5pt は見える) |
> | 事前予測 | drop ≒ 0.575 × X、**検出下限は注入率 5.20%** |
> | 判定 | A 0%で偽陽性なし / B 40%で陽性 / C Cochran の Q が有意 |
>
> **★ 次の一手はこれ** —— [preregister.md](../preregister.md) の
> **「実行環境」の表(⬜ が11個)を、fine-tune を1本でも走らせる前に埋める。**
> 埋め草コーパスの選定(JMMLU/MMLU と重ならない・四肢選択でない・SHA で pin できる)と
> GPU の借り先の決定がその中身。**空欄のまま実行しない。**

## 何をするか

ベンチマークの一部を意図的に学習させた**汚染モデルを自作**し、そこに contamlab を当てる。
「汚染されていないモデル」を探すのは不可能(無いことは証明できない)だが、
**汚染されていると分かっているモデルは作れる。** 先行研究の標準手順
(PaCoST は WMDP bio 1000 問で fine-tune している)。

## なぜこれが本命か

1. 公開した以上、「一度も陽性を出していない検出器」が最大の突っ込みどころになった
2. **HOLDOUT を消費しない。** 自作モデルが相手なので新規に事前登録して DEV で回せる
3. 注入量を変えた数水準を作れば**較正曲線**(入れた X / 測れた X̂)が得られる

## 副産物がおそらく本体 —— 較正済み汚染モデル一式

**注入量の ground truth 付きの汚染モデルを配っている人がいない。** 検出の論文はどれも
自前で作っては捨てている。標準として配れば後続が全員それで自分を測ることになる。

**これは問題集を持っていなくても作れる。** 前段の結論(稼げるのは問題を持っている側)と
噛み合い、立ち位置が「もう一つの検出器」(競合が強い)から
**「検出器を測る物差し」**(空いている)に変わる。

## 着手時の順序(飛ばさない)

1. ~~[preregister.md](../preregister.md) に**測る前に**判定基準を書き、コミットする~~
   → **2026-08-08 完了**(ラン `positive-control-01`)
2. ~~モデル・注入水準・n を決める(検出力ゲートを通す)~~
   → **2026-08-08 完了**(1.5B / 6アーム / n=4,742。ψ=0.80 でも 5pt が見える)
3. ⬜ **実行環境の表を埋める**(埋め草コーパス・GPU・LoRA 設定・T・E・SHA 群)
4. ⬜ fine-tune 6本 → GGUF Q4_K_M へ変換 → Ollama に載せる
5. ⬜ **操作チェック**(注入群と非注入群の原文正解率の差)。ここで落ちたら測定に進まない
6. ⬜ 測定(56,904 コール・4.7〜15.8 時間の見積り)→ `docs/run-positive-control-01.md`

**「結果を見てから規則を選ぶ」ことになるので、測定値・判定・n は後から変えない。**
これは全コミットで守ってきた規律。

**★ 5 を飛ばさない。** 注入が入っていないのに測定まで進むと、陰性の結果が
「装置が鈍い」のか「注入が入っていない」のか**永久に区別できなくなる。**

## ⚠️ 触ってはいけないもの(ステップ1から継続)

- **編集禁止領域** — `harness.py` / `stats/` 配下 / `runner.py` の採点規則 / 指標の定義 /
  DEV・HOLDOUT の分割 / 非公開シード / `clients.py`
- **出力書式 A/B/C(`runner.PROMPT_FORMATS`)は凍結。**4つ目を作らない
- **依存は標準ライブラリのみ。** 外部調査は scratchpad の使い捨て venv でやり、
  `pyproject.toml` には触れない
- **認証情報を自分で探しに行かない。** 必要なら「`.env` に設定してください」とユーザーへ依頼する。
  パスワードマネージャーには理由を問わず触れない

---

# ステップ3: 書く

**ステップ2の後。** 同じ主張でも説得力が変わる。

- 今書けること: 「100 問では 11% 以上の汚染しか見えない」—— **理屈**
- ステップ2の後: 「5% の汚染を仕込んだ。100 問では見つからなかった。**これがその記録**」—— **実物**

## 未処理の宿題

- **API キーの失効**(2026-08-08 にユーザーへ依頼済み・コンソールのみ)と `.env` の値消し
- `C:\Users\kingo\_keys-trash\20260807-downloads-pem\` に退避した SSH 秘密鍵 2 件の要否判断
  (**削除はしていない**。[_trash/README.md](../_trash/README.md) に経緯)
