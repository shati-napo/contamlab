# ラン `jmmlu-shuffle-03` の報告 —— **完走・汚染ありとは結論しない**(2026-08-07)

**結論を先に書く。**

> **`Llama-3.1-Swallow-8B-Instruct-v0.5` で約 4.2 ポイント、`llm-jp-3-13b-instruct3` で
> 約 5.1 ポイント —— この設計で検出できる下限はそこであり、それを超える正答率の低下は
> JMMLU の HOLDOUT 1,097 問では見つからなかった。**

**「JMMLU は汚染されていない」とは書かない。**有意差の不在は汚染の不在ではない。
上の下限を下回る汚染は、この設計では**そもそも見えない**。

このランは 01・02 と違い、**中止していない。**⓪ 書式選定 → ① 採点の健全性 →
決定性の実測 → ② ψ の実測 → 本番 DEV → 本番 HOLDOUT を事前確約どおり全段通し、
**規則を1つも書き換えずに結論へ到達した最初のランである。**結論が null であることと、
規律が働いたことは矛盾しない。

**HOLDOUT 1,922 問は 2026-08-07 に開封・消費した(K = 1 / 10)。**

一次記録は [preregister.md](../preregister.md) の「ラン: jmmlu-shuffle-03」節。
本稿はその要約であり、**数字が食い違ったら preregister が正**である。

---

## 1. 何を測ったか

日本語ベンチマーク **JMMLU**(53科目・6,664問)について、
**「スコアが能力なのか、問題を覚えているだけなのか」**を分離する。

方法は**摂動法**である。選択肢の並び順だけを入れ替え(`shuffle_choices`)、
中身が同じ問題で正答率がどれだけ落ちるかを見る。理解して解いているなら
並び順は効かない。位置ごと覚えているなら落ちる。

| 設計 | 値 |
|---|---|
| 分割 | DEV 4,742 問 / **HOLDOUT 1,922 問**(**公開シードで固定**。§再現するには の訂正を見よ) |
| 本番の問題数 | **各 1,097 問**(パイロット②の実測 ψ から導出。§2.4) |
| 摂動器 | `shuffle_choices`(**K = 1 / 10**) |
| 狙う効果量 | 5 pt の低下 |
| 検定 | McNemar(対応あり)+ Clopper-Pearson の厳密下限 + Holm 補正 + Cochran の Q |
| ロースター | `llmjp3-13b` / `swallow31-8b`(**M = 2 → Holm 実効 α = 0.0250**) |
| **出力書式** | **C(先頭固定)** —— **パイロット⓪が選定した。手では指定していない**(§2.1) |
| 測定条件 | `mmnga` の GGUF / Q4_K_M / temperature 0 / max_tokens 256 / `OLLAMA_NUM_PARALLEL=1` |
| 実行系 | Lambda の A100-SXM4-40GB 上の Ollama 0.32.6。**問題文はホストの外に出していない** |

**02 からの変更点は「出力書式を選び直した」ことだけである。**ベンチマーク・分割・
非公開シード・摂動器・狙う効果量・採用基準・K の上限・母集団の定義・ロースター・
量子化・提供元・`temperature` / `max_tokens` は一切動かしていない。採点器(`select`)も
2026-08-07 の修正を当てた状態で凍結した。

### 採用書式の全文(結論は書式に条件づけられている)

[runner.py:77-84](../contamlab/runner.py#L77-L84) の書式 C。問題文と選択肢の後に、
**末尾の指示行だけ**が付く。

```
最初の行に「答え: X」とだけ書いてください(X は選択肢の記号)。
理由や説明は書かないでください。
```

> [!warning] ⚠️ **書式は測定条件の一部である。**
> 本稿の結論は「JMMLU に位置の記憶があるか」ではなく、
> **「この書式で問うたときに、位置の記憶が X pt 以上あるか」**としか読めない。
> 書式は答え方を変えるだけでなく**モデルが考える量も変える** —— C は説明を禁じるので
> 推論を抑える方向に働き、抑えられた分だけ記憶に頼る度合いが上がりうる。
> **つまり書式そのものが汚染の見え方を動かしうる。**
> これは避けられないので、全文をここに載せ、**02 の数値と直接比較しない。**

## 2. 全段の記録

### 2.1 パイロット⓪ —— 書式を選ぶ(n=150 / `identity` のみ / DEV)

| 書式 | `llmjp3-13b` | `swallow31-8b` | 判定(2本とも < 5% で合格) |
|---|---|---|---|
| A(現行) | **33 / 150(22.0%)** | 1 / 150(0.7%) | ❌ 不合格 |
| B(出力例つき) | **22 / 150(14.7%)** | 5 / 150(3.3%) | ❌ 不合格 |
| **C(先頭固定)** | **2 / 150(1.3%)** | **0 / 150(0.0%)** | ✅ **合格** |

合格が1つなので C を採る(選定規則4)。

**この選定が「落ちたモデルが通る条件を、落ちたのを見てから選ぶ」ことにならないよう、
4つの仕掛けを測る前に固定してあった。**

1. **選定に使う指標は解釈不能率だけ。**正解率すら選定に使わない —— 抽出スクリプトは
   正解率を**出力しない**(`answer` にダミーを入れ、正誤を構造的に見られなくする)
2. **選定は摂動を一度も当てずに行う。**⓪ は `identity` のみで、**摂動後の応答を1件も
   見ずに書式が決まる。**選定が汚染の強さに影響される経路が構造的に存在しない
3. **選定は DEV のみ。**選定統計量(DEV・`identity`・解釈不能率)と検定統計量
   (HOLDOUT・`shuffle_choices`・`drop`)は別のデータ・別の条件で計算される。
   よって**書式を3つ試しても HOLDOUT の p 値は膨らまない**
4. **候補3つを先に凍結し、合格ゼロなら4つ目を作らず本番を中止する**と先に宣言した

**そして候補の先頭は現行書式 A であった。**「変える」と決めてから測ったのではなく、
**「変えるべきかを測った」。**A が合格していれば書式は動いていない。

> [!note] 02 の中止原因が書式であったことと整合する —— ただし**これは事後の解釈である**
> A の `llmjp3-13b` は 22.0% で、02 の再採点で見えていた 15.7%(n=70・別ホスト)と
> **同じ向き**である。しかし ⓪ は書式ごとの解釈不能率しか測っておらず、
> **02 が中止になった因果を検定してはいない。**ここで確定したのは
> 「以後は書式 C で測る」という測定条件だけである。

### 2.2 パイロット① —— 採点の健全性(n=70 / `identity`)

| モデル | 素の正解率 | 解釈不能(原 / 摂) | 判定 |
|---|---|---|---|
| `llmjp3-13b` | 0.3571 | 1 / 1(1.4%) | ✅ 通過 |
| `swallow31-8b` | 0.5000 | 0 / 0(0.0%) | ✅ 通過 |

- **床効果は無い**(両モデルとも4択の偶然正解 0.25 を上回る)
- **課金された呼び出しは 0 回。**書式 C の 150 問は ⓪ で取得済みで、
  `take_deterministic` が prefix 安定なので **70 ⊂ 150** が実測で確認された

02 で `llmjp3-13b` が解釈不能 26/70(37%)で脱落した状態とは別物になっている。

### 2.3 決定性の実測(140 回の実呼び出し)

同じ 70 問を**独立したキャッシュ**に取り直して突き合わせた。

| モデル | 共通キー | 生の文字列が不一致 | 正規化後も不一致 | 採点結果 |
|---|---|---|---|---|
| `llmjp3-13b` | 70 | **0** | **0** | 一致 |
| `swallow31-8b` | 70 | **0** | **0** | 一致 |

**→ この環境では完全決定的。ψ は非決定性で膨らんでいない。**

`temperature 0` は「最も確率の高い選択肢を選ぶ」であって「計算結果が同じになる」では
ないので、**決定性は宣言ではなく実測で示す。**そしてこの結果は保証ではない ——
測ったのは書式 C の 140 プロンプトだけで、ハード・ドライバ・Ollama・並列度のどれかが
変われば測定条件の変更であり、**そのときは再び測る。**

### 2.4 パイロット② —— ψ の実測と必要問題数(n=250 / `shuffle_choices`)

| モデル | 素の正解率 | 摂動後 | ψ̂ | 解釈不能(原 / 摂) |
|---|---|---|---|---|
| `llmjp3-13b` | 0.3560 | 0.3360 | 0.3000 | 5 / 5(2.0%) |
| `swallow31-8b` | 0.5720 | 0.5920 | 0.2360 | 2 / 2(0.8%) |
| **全体** | — | — | **0.2680** | — |

**採用 ψ = 0.3275**(実測 ψ̂ の Clopper-Pearson 両側95%上側。楽観側に外すと検出力を
失うので上側を採る規則)→ 写像表から **必要問題数 n = 1,097**。
02 の 1,270 は書式が変わった時点で失効させており、引き継いでいない。

> [!warning] ⚠️ ここで**規則の曖昧さが表に出た。そして直さなかった**
> 写像表は「**実測 ψ の** CP 上側」としか書いておらず、**モデルが2本ある場合に
> 全体を読むのかモデル別の最大を読むのかを決めていなかった。**
> `60-production.sh` は全体を読むよう**値を見る前に**実装されていたので、それに従った。
>
> モデル別の最大(0.3609 → n=1,270)に読み替えれば数字は変わるが、
> **それは値を見てから規則を選ぶことである。**02 で採点器を直したときに
> 「前例にしない」と引いた線を、ここで自分から越えない。
>
> **代償を実行前に書いてある** —— n=1,097 での `llmjp3-13b` の検出力は **0.788** で、
> 目標 0.80 をわずかに下回る。**これは結果に関わらずそのまま報告する。**

### 2.5 本番 DEV(n=1,097 / `shuffle_choices` / K=1)

3,314 回の実呼び出し。キャッシュ 1,582 → **4,896 行**(過不足なし)。`conflicts` なし。

| モデル | 素の正解率 | 摂動後 | 低下 | 割引後下限 | Holm 後 p | 判定 |
|---|---|---|---|---|---|---|
| `llmjp3-13b` | 0.3610 | 0.3428 | +1.82pt | −0.0114 | 0.3206 | — |
| `swallow31-8b` | 0.5962 | 0.5907 | +0.55pt | −0.0188 | 0.3745 | — |

不均一さ: Q = 0.3215, df = 1, **p = 0.5707**, I² = 0.000 → 有意でない。

解釈不能率は 4 条件すべて 5% 未満(1.19% / 1.37% / 0.64% / 0.73%)で、
**⓪ の書式選定は規模を 150 → 1,097 に上げても崩れていない。**

**DEV での非検出をランの結論にはしない。**DEV は「設計が動くこと」と
「書式 C が規模に耐えること」の確認であり、**汚染の判定は HOLDOUT でのみ行う。**

## 3. 本番 HOLDOUT の結果 —— このランの結論

4,304 回の実呼び出し。キャッシュ 4,896 → **9,200 行**(= 4,896 + 4,304、過不足なし)。
`conflicts` なし・警告なし。

> [!note] **★ 呼び出し回数の内訳(素朴な期待値 4,388 ではなく 4,304 である理由。2026-08-08 に確認)**
> 2 モデル × 1,097 問 × 2 条件 = **4,388** が素朴な期待値で、実測との差は **84 回**。内訳は2つ:
>
> - **41 問(3.74%)で `shuffle_choices` の置換がたまたま恒等置換になり、摂動後プロンプトが原文とバイト一致した。**
>   4択なので理論値は 1/24 = 4.17%。`CachedModel.answer` がキャッシュを返すためモデルは呼ばれない
>   (41 × 2 モデル = **82 回**)。**この 41 問は構造上かならず一致ペアになる** ——
>   位置が動いていない以上それは正しい挙動だが、**実効的な母数は 1,097 ではなく 1,056 である。**
> - **残り 2 回は、応答キャッシュ経由で DEV から HOLDOUT へ再生されたものである**(下記)。
>
> **★ 応答キャッシュは DEV と HOLDOUT を区別しない。**
> キーは `model_name + prompt` のみで、**問題 id も分割も入っていない**。JMMLU には
> プロンプトが完全一致する問題が **14 組**あり、うち **6 組が DEV / HOLDOUT にまたがる**。
> **そのうち 1 組が本番 DEV と HOLDOUT の 1,097 問の両方に入っていたため、HOLDOUT の 1 問は
> 本番 DEV 実行時のキャッシュから応答が再生され、モデルは一度も呼ばれていない。**
> `conflicts` は立たず、警告も出ない。
>
> **1/1,097 なので結論に影響しない。**記録するのは、**「HOLDOUT を一度も見ていない」が
> 実装ではなく運用で担保されている**ことの実例だからである。キャッシュは編集禁止領域ではないが、
> **本ランの数値は動かさない。**

| モデル | 素の正解率 | 摂動後 | 低下 | 95%CI | 割引後下限 | Holm 後 p | 判定 |
|---|---|---|---|---|---|---|---|
| `llmjp3-13b` | 0.3510 | 0.3519 | **−0.09pt** | [−3.60, +3.42] | −0.0305 | 0.7951 | **—** |
| `swallow31-8b` | 0.5761 | 0.5716 | **+0.46pt** | [−2.38, +3.28] | −0.0194 | 0.7951 | **—** |

(「低下」は正が下落。`llmjp3-13b` の **−0.09pt は摂動後にわずかに上がった**という意味)

> [!warning] **★ K=1 なので「割引後下限」に割引はかかっていない(2026-08-08 に明記)**
> `expected_max_of_k(1) = 0.0` である。したがって上表の「割引後下限」は
> **割引前の片側 95% Clopper-Pearson 下限そのもの**であり、E[max]/σ による摂動器選択バイアスの
> 補正は本ランでは一度も働いていない。
>
> さらに `mcnemar.py` の設計上 **`lcb > 0 ⟺ 片側厳密 p < α`** が厳密に成立し、`p_holm ≥ p` なので、
> **K=1 のとき採用基準1 は基準2 に論理的に含まれる。** 本ランで実際に働いた門は
> **「Holm 補正後 p < 0.05」と「Cochran の Q」の2つ**であり、**基準1 は独立の情報を足していない。**
> 基準1 が効き始めるのは **K ≥ 2**(摂動器を2種類目に進めたとき)である。
>
> なお **95%CI は両側**、**割引後下限は片側**である(前者は各裾に α/2、後者は α をそのまま)。
> 下限 −3.05pt が信頼区間の下端 −3.60pt の内側にあるのはこのためで、誤りではない。
>
> ⚠️ **信頼区間は不一致ペア数 n_d を固定した条件付き区間である**([mcnemar.py:107-109](../contamlab/stats/mcnemar.py#L107-L109))。
> n_d 自体のばらつきを織り込まないぶん**保守側に歪む**。装置が自ら課していた開示義務であり、ここで果たす。

- 全体 ψ̂ = **0.2762**(設計値 0.3275 の内側)、**達成検出力 0.935**
- 不均一さ: Q = 0.0594, df = 1, **p = 0.8074**, I² = 0.000 → 有意でない
- 解釈不能率: 1.73% / 2.01% / 0.64% / 0.55% → **未見の 1,097 問でも 4 条件すべて 5% 未満。
  ⓪ の選定は HOLDOUT に外挿できた**

### 採用基準の照合(4つ全部を満たしたときだけ「汚染あり」)

| # | 基準 | `llmjp3-13b` | `swallow31-8b` |
|---|---|---|---|
| 1 | 割引後下限 > 0 | ❌ −0.0305 | ❌ −0.0194 |
| 2 | Holm 補正後 p < 0.05 | ❌ 0.7951 | ❌ 0.7951 |
| 3 | 事前の検出力 ≥ 0.80 | ❌ 0.783〜0.816 | ✅ 0.922〜0.947 |
| 4 | 不均一さで難易度上昇を排除 | ❌ p = 0.8074 | ❌ p = 0.8074 |

**1・2・4 が未達。したがって「汚染あり」とは結論しない。**

> [!note] **★ 基準3 の判定を `△` から `❌` に改めた(2026-08-08。判定は変わらない)**
> 事前確約は「**4つを全部満たしたときのみ**汚染ありと結論する」であり、**基準は二値である。**
> `llmjp3-13b` の 0.783〜0.816 は目標 0.80 を挟むが、**「挟んだ」は「満たした」ではない。**
> 規則どおり未達として扱う。**事前確約に存在しない第3の記号を発明したのは誤りだったので訂正する**
> (基準1・2・4 が既に未達なので、**結論への影響はない**)。
>
> **この検出力不足は開封前に preregister へ記載済みであり、結果を見て事後に判明したものではない。**
> 全体 ψ̂ を読む実装は commit `bc519db`(**2026-08-04 22:17**)で導入され、
> パイロット②の実行は **2026-08-07** である。**実装が測定値に3日先行していることは
> `git log -S "observed_discordant_rate" -- scripts/60-production.sh` で第三者が独立に確認できる。**
> 規則の是正(モデル別 ψ の CP 上側の最大で引く → n=1,270)は 2026-08-08 に確定し、
> **適用は次のランから**である。遡及適用しないのは、それが「結果を見てから規則を選び直す」に当たるため。

### 開封を決めたのは人間である

DEV は両モデルとも検出なしで終わっており、**HOLDOUT も同じ結果になる公算が高いことを
承知のうえで開封した。**「有意になりそうだから撃つ」ではなく
**「事前確約を全段通したので、宣言どおり最後まで実行する」**という理由である。
構成(分割・摂動器・シード・問題数・書式・モデル・効果量・ψ)は**撃つ前に
preregister へ記載済み**(`977fc15`)で、**DEV と違うのは分割だけ**である。

## 4. 何が言えて、何が言えないか

**言えること。**

- この設計で検出できる下限は `swallow31-8b` で約 **4.2 pt**、`llmjp3-13b` で約 **5.1 pt**
  (ψ の CP 上側・検出力 0.80・Holm 実効 α=0.0250 で計算)であり、
  **それを超える正答率低下は HOLDOUT 1,097 問では見つからなかった**
- 書式 C は 150 問 → 1,097 問 → 未見の HOLDOUT 1,097 問すべてで解釈不能率 5% 未満を保った
- この環境・この書式・この 140 プロンプトについて、両モデルは決定的だった

**言えないこと(重要)。**

- **「JMMLU は汚染されていない」。**有意差なしは汚染の不在ではない。
  4.2pt / 5.1pt を下回る汚染はこの設計では見えない
- **`llmjp3-13b` については特に弱い。**検出力は 0.783〜0.816 で目標 0.80 を挟んでおり、
  **開封前に宣言したとおり**「この検出力では検出できなかった」としか書けない。
  `swallow31-8b`(0.922〜0.947)の非検出のほうが証拠として強い
- **仮に有意になっていたとしても「汚染」とは言えなかった。**不均一さが有意でない以上、
  一律の低下は「摂動が難易度を上げただけ」と区別できない(採用基準4)。
  **対照モデル(汚染されていないと分かっているモデル)が無いのが、この設計の限界である**
- **「惜しかった」とは書かない。**低下は −0.09pt と +0.46pt で、惜しくもない
- **位置の記憶以外の汚染について何も言っていない。**測ったのは `shuffle_choices` 一種類だけで、
  問題文の言い換えや数値差し替えに対する記憶は**一度も試していない**(K は 1 しか使っていない)
- **02 の数値との比較。**書式が違うので、正解率も ψ も直接は比べられない

> [!warning] **★ 採用基準4 の検出力は 0.80 ではない —— 律速はここだった(2026-08-08 に計算)**
> **事前に確約した検出力 0.80 は、McNemar 検定(採用基準1・2)についてのものである。**
> しかし**採用基準は AND** なので、「汚染あり」に到達する確率は**最も検出力の低い基準に支配される。**
> **基準4(Cochran の Q)の検出力は、標本サイズの導出に一度も入れていなかった。**
>
> 上表の 95%CI(両側)から標準誤差を復元すると、SE(`llmjp3-13b`)= **1.79pt** /
> SE(`swallow31-8b`)= **1.44pt**、モデル間の低下幅の差の SE = **2.30pt**。df=1・両側 α=0.05 で:
>
> | モデル間の低下幅の差(真値) | Q が p<0.05 になる確率 |
> |---|---|
> | 3.0 pt | 0.257 |
> | 4.0 pt | 0.413 |
> | **5.0 pt(狙う効果量と同じ)** | **0.585** |
> | 6.44 pt | 0.800 |
>
> **つまり「1本だけがちょうど 5pt 落ち、もう1本は全く落ちない」という、この設計が想定する
> 最も分かりやすい汚染ですら、複合判定の検出力は約 0.6 にとどまる。** 0.80 には 6.44pt が要る。
> Q が有意になる最小の**観測**差は 4.51pt である。
>
> **事前確約に基準4 を置きながら、その検出力を問題数の導出に入れていなかったのは設計の穴である。**
> このプロジェクトは「検出力分析を欠く汚染研究」を批判する立場に立っているので、**自分から書く。**
> **次のランでは n を McNemar ではなく基準4 の検出力で決める。**
>
> **加えて、Cochran の Q は独立な研究を前提とするが、本設計の2本は同一の 1,097 問・
> 同一の摂動実現値を解いており、低下幅は正に相関する。** 相関を無視すると差の分散を
> 過大評価するので、**Q は保守側(不均一さを検出しにくい側)に歪む。**
> `stats/heterogeneity.py` は編集禁止領域なので本ランでは直さない。
> 歪みの向きは「汚染ありと結論しにくくなる」側なので、**本ランの null 結論を弱める方向には働かない。**
>
> **M=2 に固有の限界も併せて書く。** Q が有意になっても、**2本のうちどちらが汚染側かは Q からは分からない。**
> 「A が記憶で落ちた」と「B が摂動に異常に強い」は同じ Q を生む。方向を決めるには
> 外部の仮定(対照モデル・コーパス照合)が要る。**M=2 では基準4 は「一律低下の排除」以上を言えない。**

> [!note] **★ 「4.2pt / 5.1pt」がどの ψ で計算されたか(2026-08-08 に明記)**
> ψ は**本番 HOLDOUT の実測 ψ̂ のモデル別 Clopper-Pearson 上側**を使った。すなわち**事後量**である。
> **事前確約した設計値(プール ψ = 0.3275)で計算すると、両モデルとも 4.84pt になる。**
> `swallow31-8b` については事後値のほうが有利(小さい)、`llmjp3-13b` については不利(大きい)
> なので系統的な偏りではないが、**どちらで計算したかを書いていなかったのは再現性の穴である。**
> **どちらの読み方でも本ランの結論は変わらない**(−0.09pt / +0.46pt はいずれの下限にも遠く及ばない)。
>
> ⛔ **「検出できる下限」を「汚染の上限」と読み替えてはならない。** これは検出力 0.80 での
> 最小検出可能効果量であって、「これを超える汚染は必ず捕まえた」という意味ではない。
> **真に 5pt の汚染があっても、見逃す確率は約 20% ある。**

> [!note] **★ 「完全決定的」は言い過ぎだった(2026-08-08 に訂正)**
> 実測は **0/140** であり、片側 95% Clopper-Pearson では**非決定率 2.1% まで**
> (モデル別に見れば 0/70 で **4.2% まで**)を否定できていない。n=1,097 に外挿すれば
> **最大 23 件/モデル/条件のゆらぎと両立する。**
> さらに、測ったのは `identity` の**原文プロンプト 140 件のみ**で、**摂動後のプロンプトの
> 決定性は一度も測っていない。** 加算順序のゆらぎが効くのは「モデルが迷っている問題」であり、
> それは `shuffle_choices` で答えが変わる問題と重なる —— **測るべき集合を測っていない。**
> **結論への実害は無い**(非決定性は対称なので `drop` を偏らせず ψ を膨らませるだけで、
> その ψ は実測値として検出力計算に入っている)が、**書けるのは
> 「この 140 プロンプトについて不一致は 0 件だった」までである。**

## 5. このランで効いた設計

**選定と検定を、データ・条件・指標のすべてで分離したこと。**書式を3つ試したのに
HOLDOUT の p 値が膨らまないのは、選定が DEV・`identity`・解釈不能率だけで閉じているからで、
これは訓練/検証の分離と同じ論法である。**「試した回数」を数えて割り引く(K)のとは別の、
もう一段の防御になっている。**

**判定を、測る前に固定した数字だけに委ねたこと。**02 では原因の説明を2回続けて外したのに
判定は一度も動かなかった。03 でも同じ形が出ている —— 写像表の曖昧さに気づいたとき、
**気づいた時点で既に実装が片方を選んでいたので、そちらに従った。**
規則を後から選べる状態にしていなかったことが効いた。

**書けない欄を作らなかったこと。**パイロット⓪の抽出スクリプトは正解率を出力しない。
転記しようとしても手元に無い。**規律を意志ではなく装置に持たせている。**

## 6. 次に決めること

**HOLDOUT は使い切った。**分割は凍結されており、「1構成・1回だけ開封」という規則の
とおり、**同じ HOLDOUT でもう一度検定することはできない。**
したがって次のランは、以下のどれかを**測る前に**選んで preregister に書く必要がある。

| 選択肢 | 内容 | 注意 |
|---|---|---|
| **A. 別のベンチマーク** | JGLUE / JCommonsenseQA 等で新しい DEV / HOLDOUT を切る | 新しい非公開シードが要る。JMMLU の manifest と同じ来歴記録を作る |
| **B. 摂動器を増やす** | `shuffle_choices` 以外(言い換え・数値差し替え)。K を 2 本目以降に進める | **HOLDOUT が無いので A と組み合わせるしかない** |
| **C. 対照モデルを用意する** | 採用基準4の限界(一律低下と汚染を区別できない)を構造的に埋める | ここが最大の弱点。ただし「汚染されていないと分かっているモデル」の定義自体が難問 |
| **D. ここで公開する** | 03 の結果を1本の文書として外に出す | **成果物を先に固定するという当初の約束はここで果たされる** |

> [!done] ✅ **写像表の負債は 2026-08-08 に解消した(§2.4)**
> **n は「モデル別 ψ̂ の Clopper-Pearson 上側」の最大値で引く**という規則に確定し、
> `60-production.sh` をそれに合わせた([preregister.md](../preregister.md) の
> 「★ M ≥ 2 のとき、どの ψ で写像表を引くか」)。
> 導出は **2026-08-04 に固定した採用基準がモデル単位である**ことだけから出ており、
> **このランの実測値は1つも入っていない。**
>
> **適用は次のランから。このランの n = 1,097 は動かしていない。**
> 参考として、03 のパイロット②に新しい規則を当てると n = 1,270 となり、
> 両モデルとも検出力 0.844 / 0.909 で 0.80 を満たす。
> **これは規則の検算であって、本稿の結論には反映しない** ——
> 03 の `llmjp3-13b` は 0.783〜0.816 で走ったという事実がそのまま残る。

> [!danger] 同じ罠が再発しうる
> 「検出手法の妥当性を検証する基盤」を作り始めたら、それは測定の測定の測定である。
> **D(公開)を A・B・C より先に置くこと。**装置の改良は成果物が出た後にする。

---

## 付録: 数字の出どころ

| 値 | 出どころ |
|---|---|
| パイロット⓪〜②・本番 DEV / HOLDOUT の生の結果 | `reports/lambda-a100-40gb-20260807/*.json`(gitignore 対象・ローカルのみ) |
| 生の応答 9,200 件 | `data/cache/responses.lambda-a100-40gb-20260807.jsonl`(同上) |
| 採用書式 | `reports/lambda-a100-40gb-20260807/prompt-format`(中身は `C`)/ [runner.py:77-84](../contamlab/runner.py#L77-L84) |
| ベンチマークの由来(53科目・7,097行から 6,664 問を採用) | [data/jmmlu.manifest.json](../data/jmmlu.manifest.json)(**git 追跡下**。取得元 commit と各 CSV の sha256 が入っている) |
| 分割 DEV 4,742 / HOLDOUT 1,922 | 上の manifest + **公開の salt**(`contamlab/benchmark.py:37,42`。**追跡下にある**) |
| 実行環境(GPU / Ollama 版 / GGUF の SHA256) | [preregister.md](../preregister.md) の「実行環境(lambda-a100-40gb-20260807)」節 |
| 検定・下限・補正・検出力の実装 | `contamlab/harness.py` / `contamlab/stats/`(**編集禁止領域**) |

`reports/` と `data/cache/` は **gitignore 対象**なので、リポジトリには入っていない。
**数字そのものは preregister.md に転記済み**であり、そちらが公開側の一次情報である。

### 再現するには

問題インスタンスは配らない(配れば次世代モデルの訓練データに混ざり、
**検査器として二度と使えなくなる**)。配るのは **manifest + 決定論的生成器 + 公開シード**であり、
**DEV も HOLDOUT も誰でも再現できる。**

> [!warning] ⚠️ **訂正(2026-08-08)— 本ランに非公開シードは使われていない**
> 本節はかつて「HOLDOUT の再現には非公開シードが要る」と書いていた。**それは誤りなので撤回する。**
>
> 分割(`SPLIT_SALT`)・抽出(`SAMPLE_SALT`)・**摂動シード(`dev-seed`)はいずれも公開値**である
> ([benchmark.py:37,42](../contamlab/benchmark.py#L37) / [lib.sh:36](../scripts/lib.sh#L36))。
> [60-production.sh:178](../scripts/60-production.sh#L178) は `--split holdout` のときも同じ `$DEV_SEED` を渡す。
> 本稿 §3 の開封記録が「シード: `dev-seed`(DEV と同一)」と書いているとおりである。
>
> **したがって本リポジトリと原典コミット `762cbf19` だけで、HOLDOUT 1,097 問の摂動済み集合は
> 第三者が完全に再構成できる。** 研究上の実害はほぼ無い —— JMMLU は元から公開されており、
> 摂動は決定論的な並べ替えにすぎない。**訂正するのは、達成していない秘匿性を読者に
> 約束していたからである。**
>
> **HOLDOUT を守っていたのは「まだ問い合わせていない」という事実だけだった。**
> 「1構成・1回だけ開封」は外部からの汚染を防ぐ機構ではなく、
> **著者自身の多重検定を防ぐ規律**である。前者を主張するには、原典を公開しない独自問題集と、
> 実際に秘匿された摂動シードの両方が要る。
